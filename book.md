
[__SOURCE](README.md)
# ${cont_model} 제어기 기능설명서 - 센서 기반 힘제어 

[__SOURCE](0-about-this-manual/precautions.md)
# 사전 주의사항

{% include url="https://hrcontentsrelay-bmgae5hdbzapc4bc.koreacentral-01.azurewebsites.net/api/proxy?path=doc-common-pages/ko/precautions.md" %}

[__SOURCE](1-intro/README.md)
# 1. 개요

본 매뉴얼은 센서 기반 힘 제어 시스템의 사용 방법을 안내하기 위한 문서입니다.  
본 시스템은 로봇에 장착된 힘/토크 센서를 활용하여 외력에 반응하는 제어를 수행하며,  
보다 정밀하고 안전한 접촉 작업을 가능하게 합니다.

본 매뉴얼에서는 다음과 같은 기능을 다룹니다

- 센서 환경 설정 방법 
- 힘 제어 조건 설정 항목 
- 명령문 
- 모니터링 구성 
- 기본 예제

이 문서는 사용자 또는 유지보수 엔지니어가  
기능을 설정하고 동작 상태를 이해하는 데 필요한 모든 절차를 설명합니다.

해당 기능은 V60.32-01 버전 부터 사용 가능하고, 기능 라이센스가 별도로 필요 합니다. 
[__SOURCE](2-settings/README.md)
# 2. 설정

센서 기반 힘 제어 기능을 사용하기 위해 아래의 기본 항목을 먼저 구성해야 합니다. 
해당 항목들은 UI를 통해 입력되며, 모든 제어 기능의 기반이 됩니다. 

- 힘 센서 좌표계
- 힘 제어 환경 구성
- 힘 제어 툴 정보  
- 힘 제어 조건  
[__SOURCE](2-settings/1-force-sensor-coordinate-system.md)
## 2.1 힘 센서 좌표계 

FT 센서를 장착할 때, 센서 좌표계는 반드시 로봇 모델과 일치하도록 설정해야 합니다. 

센서 좌표계 오설정 시, 측정된 힘/토크 방향이 실제와 달라져 제어 성능이 크게 저하됩니다.

<br>


---

### **로봇의 센서 좌표계**


> 자사에서 설정한 센서 좌표계 X, Y, Z 축은 아래  그림과 같이 정의됩니다: 

> 센서 모델에서 설정된 X, Y, Z 방향과 로봇의 센서 좌표계 방향이 동일해야 합니다. <br>


![](../_assets/_05_fctrl_ctrl_sensor_crd.png)


⚠️ 위 그림에서 로봇은 기본 자세입니다. 

⚠️ 일반적인 원형 FT 센서의 좌표계는 모델 옆면에 표시가 되어 있습니다. <br>

<br>

### 💡 설정 팁

- **좌표계가 반대일 경우**: 축 반전을 고려해 소프트웨어에서 변환해야 함
- **툴 무게 중심 입력 시**: 센서 기준 좌표계로 입력할 것
- **센서 제로 보정** 전, 올바른 방향인지 확인할 것

---

[__SOURCE](2-settings/2-force-control-environment-settings.md)
## 2.2 힘 제어 환경 설정

센서 기반 힘 제어 기능을 사용하기 위해 아래의 주요 항목들을 설정해야 합니다.  
이 항목들은 제어기의 모든 동작의 **출발점**이 됩니다. 

힘 제어 환경 설정은 아래 경로를 통해 진입할 수 있습니다: 

📂 시스템 → ⚙️ 4: 응용파라미터 → 💪 17: 힘제어 → 🛠 1: 환경 설정

<br>

---

### **기능 사용**

센서 기반 힘 제어 기능의 사용 여부를 설정합니다.

- `유효`: 기능 **활성화** 
- `무효`: 기능 **비활성화** 

> ⚠️ 반드시 Enable로 설정해야 힘 제어 기능이 작동합니다.

---

### **센서 제조사 및 모델**

사용할 **힘 센서의 제조사 및 모델**을 선택합니다.

- 예시:
  - `ATI : 일반 ATI 모델`
  - `ATI : Delta-SI-660-60`
  - `ATI : Theta-SI2500-400`
  - `ATI : Omega-SI7200-1400` 
  - `OnRobot : HEX-E` 
  - `Robotiq : FT-300S` 

센서에 따라 **데이터 포맷 및 통신 방식**이 달라지므로 정확히 선택해야 합니다.

> 💡 **참고 사항**  
> 다음 ATI 모델은 **V60.32-05 이상 버전**에서 지원됩니다.  
> - 일반 ATI 모델  
> - Theta-SI2500-400  
> - Omega-SI7200-1400

--- 

### **통신 프로토콜**

선택한 센서에서 제공하는 **통신 방식**을 설정합니다.

- `UDP`
- `SCI`
- `TCP`

⚙️ 프로토콜에 따라 포트 설정 및 내부 파싱 방식을 아래와 같이 설정

--- 
### **ATI 모델별 설정 방법**

- **통신 프로토콜** : `UDP`
- **IP 주소** : `192.168.1.2`
- **원격 포트** : `49152`


<br>

#### ✔️ **ATI(Delta-SI-660-60)**  

![](../_assets/_01_01_fctrl_env_setting_ATI_Delta_UDP.png)

<br>

#### ✔️ **ATI(Theta-SI2500-400)**
![](../_assets/_01_02_fctrl_env_setting_ATI_Theta_UDP.png)

<br>

#### ✔️ **ATI(Omega-SI7200-1400)**
![](../_assets/_01_03_fctrl_env_setting_ATI_Omega_UDP.png)

<br>

#### ✔️ **ATI(일반 ATI 모델)**
![](../_assets/_01_04_fctrl_env_setting_ATI_Generic_UDP.png)

- **일반 ATI 모델**
  - 모델을 별도로 개별 등록하지 않고, **ATI에서 제공하는 모델별 스케일링 값을 사용자가 직접 수동 입력하여 적용하는 방식**입니다.

---

### **OnRobot HEX-E 설정 방법**

- **통신 프로토콜** : `UDP`
- **IP 주소** : `192.168.1.1`
- **원격 포트** : `49152`

![](../_assets/_02_fctrl_env_setting_OnRobot_HEX_E_UDP.png)


---

### **SCI 통신 방식 : Robotiq(FT-300S)**
![](../_assets/_03_01_fctrl_env_setting_Robotiq_FT300S_SCI.png)

📂 시스템 → 🔧 2: 제어파라미터 → 🔌 3: 시리얼포트 → 🛠 1: 환경설정

![](../_assets/_03_02_fctrl_env_setting_Robotiq_FT300S_SCI_CFG.png)

---

<br>

> ⚠️ **주의 사항**
>  
> - 본 설정은 *일반적인 모델 기준 예시*입니다.  
> - **ATI 일부 모델은 상기 설정과 상이한 네트워크 값을 사용할 수 있습니다.**  
>  
> 👉 적용 전 모델별 매뉴얼 확인을 권장합니다.

[__SOURCE](2-settings/3-tool-information-settings-for-force-control.md)
## 2.3 힘 제어 툴 정보

로봇에 장착된 툴의 물리적 특성을 입력하여, 힘 제어 알고리즘의 정확도를 향상시킵니다.  
센서가 인식하는 힘과 토크는 툴 무게 및 중심 좌표에 따라 보정됩니다. 

힘 제어 툴 정보 설정은 아래 경로를 통해 진입할 수 있습니다: 

📂 시스템 → ⚙️ 4: 응용파라미터 → 💪 17: 힘제어 → 🧰 2: 툴 정보 설정

<br>


---

![](../_assets/_04_fctrl_ctrl_tool_data.png)

| 항목         | 설명 |
|--------------|------|
| **Name**      | 툴 데이터 이름 (자동 입력) |
| **Description** | 툴 설명 또는 용도 (선택 입력) |
| **Weight [kg]** | 툴의 무게. 힘 오차 계산 시 중력 보정에 사용됨 |
| **Center [X, Y, Z]** | 툴의 무게중심 위치 (센서 기준, 단위: mm) |
| **Sensor coordinate** | 센서 기준 좌표계 방향 (UI 오른쪽 그림 참고) |

> 💡 툴 정보는 최대 10개 까지 설정 가능 합니다. 

> 💡 Hi5a에 제공된 센서기반부하추정 기능은 미지원 
[__SOURCE](2-settings/4-condition-settings/README.md)
## 2.4 힘 제어 조건 설정

힘 제어 기능을 사용하기 위해서는 작업 조건에 맞는 제어 조건(목표 힘, 제어 방향, 민감도 등)을 정확하게 설정해야 합니다.  

이 설정은 로봇이 외력에 반응하는 방식과 민감도를 결정하며, 작업 품질에 직접적인 영향을 줍니다.

힘 제어 조건 설정은 아래 경로를 통해 진입할 수 있습니다: 

📂 시스템 → ⚙️ 4: 응용파라미터 → 💪 17: 힘제어 → 🎛 4: 조건 설정 


[__SOURCE](2-settings/4-condition-settings/1-basic.md)
### 2.4.1 힘 제어 조건 설정-기본

작업에 따라 제어할 축을 선택하고, 축별로 힘 목표값, 강성(Stiffness), 속도, 위치 제한(Pose limit)을 설정합니다.  
이 항목은 힘 제어의 핵심이며, 실제 로봇 동작의 반응성을 결정합니다.

<br>


---

![](../_assets/_06_fctrl_ctrl_cnd_default.png)

<br> 

#### **기본(Default) 설정 항목**

| 항목             | 설명 |
|------------------|------|
| **이름(Name)**       | 조건 이름 (예: `cnd_12`) - 목록에서 선택하여 자동 입력 |
| **설명(Description)**| 작업 설명 (예: `Sanding`) - 현재 조건의 용도 구분용 |
| **좌표계(Crd)** | 힘 제어가 적용될 좌표계 선택<br>• `Base` (베이스 좌표)<br>• `Robot` (로봇 좌표)<br>• `Tool` (툴 좌표)<br>• `User` (사용자 정의 좌표) |
| **사용자 좌표계(UCS ID)**     | 사용자 정의 좌표계 번호 - Crd가 `User`일 경우 사용 |
| **툴 좌표계(Tool ID)**    | 현재 사용하는 툴 번호 (툴 무게, 중심 정보 연결)<br>  |
| **영점 기능(Zeros)**      | 힘 센서 초기값 보정 (영점 설정)<br>• `On`: 제로 보정 실행<br>• `Off`: 원래 값 유지 |

>💡 사용자 좌표계를 선택하고, 존재 하지 않는 사용자 좌표계 번호(ID)를 입력하고 힘 제어 동작시 에러를(E1336) 출렵 합니다. 

>💡 사용자 좌표계를 선택하고, 사용자 좌표계 번호(ID)를 0으로 입력하면 로봇 좌표계와 동일 합니다. 

>💡 툴 좌표계 번호(ID)는 "📂 시스템 → ⚙️ 4: 응용파라미터 → 💪 17: 힘제어 → 🧰 2: 툴 정보 설정" 에서 설정된 툴 번호를 의미 합니다

>💡 영점 기능을 사용 하지 않으면(`Off`), 센서에서 출력된 값에 설정된 툴 번호 정보(`무게 및 중심`)를 기준으로 보정된 값을 출력 합니다. 

<br> 

#### 기본 항목 설정 예시

> 예: `Sanding 작업용 조건`
>
> - **이름(Name)**: `cnd_12`  
> - **설명(Description)**: `Sanding`  
> - **좌표계(Crd)**: `Tool` 좌표계로 선택  
> - **툴 좌표계(Tool ID)**: `0`번 힘 제어 툴 정보(질량/중심) 사용  
> - **영점 기능(Zeros)**: `ON` (힘 제어 동작 시 0으로 초기화된 센서 데이터값 사용)  

---
[__SOURCE](2-settings/4-condition-settings/2-control.md)
### 2.4.2 힘 제어 조건 설정-제어

힘 제어 축을 선택하고, 방향별 힘 목표값, 강성(Stiffness), 속도 및 위치 제한값을 설정합니다. 

이 항목은 힘 제어의 핵심이며, 실제 로봇 동작의 반응성을 결정합니다.

<br>

---

![](../_assets/_07_fctrl_ctrl_cnd_control.png)


#### **제어(Control) 설정 항목**

| 항목       | 설명 |
|------------|------|
| **Axis**    | 제어 가능한 축 (X, Y, Z, Rx, Ry, Rz) |
| **Act**     | 해당 축의 제어 활성화 여부 (`✓` 시 활성) |
| **Force / Torque** | 목표 힘 (N) 또는 토크 (Nm)<br>예: Z축 50N 설정 |
| **Stiff**   | 강성 비율 (%), 낮을수록 유연하게 반응 |
| **Vel**     | 힘 제어 중 이동 속도 제한 (mm/s 또는 deg/s) |
| **(-)Pose / (+)Pose** | 음/양 방향 위치 한계 (mm 또는 deg)<br>초과 시 로봇 동작 제한 |

---


#### 예시 설정 해석

<br> 

> 💡 아래 설정은 **수직 방향 샌딩** 작업을 위한 조건입니다:

| 축   | 제어 | 힘 | 강성 | 속도 | 위치 제한 |
|------|------|----|------|------|-------------|
| **Z**   | `✓` | 50N | 30% | 20 mm/s | -50 ~ +50 mm |
| **Rx**  | `✓` | 0 Nm | 10% | 5 deg/s | -10 ~ +10 deg |
| **Ry**  | `✓` | 0 Nm | 10% | 5 deg/s | -10 ~ +10 deg |
| **X**, **Y**, **Rz** | - | - | - | - | - | 

→ 로봇은 Z축 방향으로 50N의 힘을 유지하며, 툴 회전 방향(Rx, Ry)은 유연하게 대응합니다.

---


축별 세부 제어는 실제 작업 조건(예: 표면 곡률, 정밀도 요구도 등)에 따라 조정해야 하며,  
Gain이 낮을수록 더 유연한 반응을 얻을 수 있습니다.


> ⚠️ 강성 비율을 낮으면 유연한 반응을 얻지만, 로봇 응답과 환경에 따라 진동과 소음을 유발 할 수 있습니다.
[__SOURCE](2-settings/4-condition-settings/3-filtering.md)
### 2.4.3 힘 제어 조건 설정-필터링

힘 센서의 노이즈를 줄이고, 입력값을 안정적으로 제어에 사용할 수 있도록 하기 위한 필터링 기능을 제공합니다.

또한, 일정 힘/토크 범위 내에서 제어 강도를 조절할 수 있는 스케일링 옵션과 

힘 입력에 대한 반응을 더욱 민감하게 하는 바이패스 옵션도 제공합니다. 

<br>


---

![](../_assets/_08_fctrl_ctrl_cnd_filtering_smooth_force.png)

#### 힘 필터링 [Smooth Force]

센서의 힘 신호에 대해 **필터** 를 적용하여 흔들림을 제거하고 부드럽게 만듭니다.

| 항목 | 설명 |
|------|------|
| **활성화 여부** | 체크 시 필터 적용 |
| **주파수(Freq)** | 필터 컷오프 주파수 설정 (예: 20 Hz)<br>→ 낮을수록 부드러우나 응답이 느려짐 |

<br>

---

![](../_assets/_09_fctrl_ctrl_cnd_filtering_smooth_scaling.png)


#### 힘 스케일링 [Smooth Scaling]

특정 힘 또는 토크 범위 내에서 제어 강도를 부드럽게 스케일링하는 기능입니다.  
민감도 조정이 필요한 작업에서 유용하며, 보통은 꺼둡니다.

미세한 힘 변화에는 제어를 억제하고, 일정 수준 이상의 힘에만 민감하게 반응하도록 설계가 가능합니다. 

| 항목 | 설명 |
|------|------|
| **활성화 여부** | 체크 시 아래 범위 내에서만 스케일 적용 |
| **Force Range** | 힘 범위 (예: 2 ~ 8 N) |
| **Torque Range** | 토크 범위 (예: 0 ~ 0 Nm) |

![](../_assets/_11_fctrl_ctrl_cnd_filtering_scaling_graph.png)

>💡 `힘 범위` 입력시 시작 힘보다 작은 경우 출력 힘은 0으로 출력 되고, 종료 힘 보다 큰 경우 원래 입력된 힘의 값으로 출력된다. 


> ⚠️ 시작 힘과 종료 힘이 같으면 스케일링 기능은 동작 하지 않는다. 

<br>

--- 


![](../_assets/_10_fctrl_ctrl_cnd_filtering_cmd_flow.png)

#### 명령 방식 [Command Flow]

로봇에 전달되는 명령 신호 **처리 방식**을 설정합니다.

| 항목 | 설명 |
|------|------|
| **Mode** | `Normal`: 일반적 명령 전달 방식 <br>`Bypass`: 민감한 반응을 위한 명령 전달 방식  |
| **Freq** | `Bypass` 모드에서 사용하는 필터 주파수 설정 (Hz) |

> **Bypass** 모드는 **센서 데이터를 즉시 반영**하고자 할 때 사용하며,  
> 매우 민감한 제어가 필요한 실험적 상황에서만 사용 권장됩니다.  
> **소음이나 진동의 위험**이 있으므로 일반 작업에는 **Normal** 모드 사용을 권장합니다.


--- 

<br>


### 설정 가이드

| 작업 유형        | 권장 설정                   |
|------------------|-----------------------------|
| 폴리싱/샌딩      | `Smooth Force = ON, Freq = 20 Hz` |
| 부드러운 조립    | `Smooth Scaling = ON, 보수적 범위 설정` |
| 고속 반응 / 실험 | `Command Flow = Bypass` |

---

📎 **필터링**, **스케일링**, **명령 흐름** 설정은 모두 함께 작용하므로, 작업 목적에 따라 **통합적으로 튜닝**해야 합니다.

[__SOURCE](2-settings/4-condition-settings/4-motion/README.md)
### 2.4.4 힘 제어 조건 설정-모션   

힘 제어 동작 시 함께 수행할 모션 제어 조건을 설정할 수 있습니다.  

**접촉면 판단** 기준을 설정하여 툴 방향 기준 표면에 접촉이 완료 되었는지를 판단 할수 있습니다. 또한, 

**move 명령어**를 사용하지 않고 **나선형/양방향/지그재그** 경로와 궤적을 자동으로 생성하여 작업이 용이하게 가능하도록 합니다. 

[__SOURCE](2-settings/4-condition-settings/4-motion/1-contact.md)
#### 2.4.4.1 힘 제어 조건 설정-모션-접촉면 판단    

힘 제어 동작(`툴 Z 방향`) 시 로봇이 표면에 제대로 접촉 했는지를 실시간으로 판단하는 기능


---


![](../_assets/_12_fctrl_ctrl_cnd_motion_contact.png)


<br>

### 접촉면 판단 기준 설정 

아래 항목이 모두 만족하면 물체에 접촉 완료 되었다고 판단  

| 항목명       | 설명 |
|--------------|------|
| **Force Thresh**      | 목표 힘과 현재 힘의 오차 임계값 (단위: N)|
| **Dev Angle Thresh**    | 툴 좌표 기준 Z 방향 힘 방향의 순간 변화량 (단위: deg) |
| **Tilt Angle Thresh** | 접촉면과 힘 방향이 이루는 각도 (단위: deg) |
| **Confirm time**  | 지속 시간 (단위: sec) |

---

<br>

### 설정 예시 

> 예: 3초 동안 아래 기준으로 접촉을 유지하면, 접촉면 판단 OK

| 항목             | 설정 값 |
|------------------|---------|
| 힘 오차           | `3 N` |
| 힘 방향 변화량      | `20 deg` |
| 접촉 각도      | `30 deg` |
| 유지 시간             | `3 sec` |

<br>

📌 `접촉면 판단 기준` 기능을 올바르게 사용하기 위해서는, 선택된 좌표계가 **툴 좌표계** 기준으로 설정되어야 한다.   
[__SOURCE](2-settings/4-condition-settings/4-motion/2-raster.md)
#### 2.4.4.2 힘 제어 조건 설정-모션-자동 경로 생성  

힘 제어 동작 시 함께 수행할 모션 제어 조건을 설정할 수 있습니다.  

특히 **나선형/양방향/지그재그**  경로와 궤적 설정이 이곳을 통해 이루어집니다.


<br>

---

![](../_assets/_13_fctrl_ctrl_cnd_motion_raster1.png)

![](../_assets/_15_fctrl_ctrl_cnd_motion_raster3.png)


### 모션 타입 [Motion Type]

| 항목명         |
|----------------|
| **나선형 모션** <br> |
| **양방향 모션**  <br> |
| **지그재그 모션**  <br> |

<br>

---

![](../_assets/_14_fctrl_ctrl_cnd_motion_raster2.png)

<br>

### 나선형 모션[Spiral Motion]

나선형 경로와 궤적을 생성하는 기능입니다.  

| 항목명       | 설명 |
|--------------|------|
| **Velocity**  | 회전 선속도 (mm/s) |
| **Radius**    | 최대 반지름 설정 (mm) - 최종 나선의 크기 |
| **Revolutions** | 회전수 (rev) - 전체 몇 바퀴 도는지 |


### 양방향 모션[Bidirectional Motion]

양방향 경로와 궤적을 생성하는 기능입니다.  

| 항목명       | 설명 |
|--------------|------|
| **Velocity**  | 회전 선속도 (mm/s) |
| **Mov Dir**    | 이동 방향  (+X, -X, +Y, -Y) |
| **Mov Length** | 이동 길이 |
| **Shift Dir**    | 줄 바꿈 방향  (+X, -X, +Y, -Y) |
| **Shift Length** | 줄 바꿈 길이 |
| **Num Lines**    | 이동 방향 기준 생성 되는 줄의 개수 |

### 지그재그 모션[ZigZag Motion]

지그재그 경로와 궤적을 생성하는 기능입니다.  

| 항목명       | 설명 |
|--------------|------|
| **Velocity**  | 회전 선속도 (mm/s) |
| **Mov Dir**    | 이동 방향  (+X, -X, +Y, -Y) |
| **Mov Length** | 이동 길이 |
| **Shift Dir**    | 줄 바꿈 방향  (+X, -X, +Y, -Y) |
| **Shift Length** | 줄 바꿈 길이 |
| **Num Lines**    | 이동 방향 기준 생성 되는 줄의 개수 |


<br>

---


📌 해당 모션은 2.4.1에서 선택된 좌표계 기준으로 XY 평면 기준으로 경로를 생성 합니다.  
[__SOURCE](3-roblang/README.md)
# 3. 힘 제어 명령어

이 문서는 힘 제어  기능과 관련된 주요 명령어에 대한 설명을 제공합니다.  
각 명령어는 힘 제어 설정, 동작 시작/종료, 레스터 모션 생성 등에 사용됩니다. 

<br>

---

### Force Control 명령어 목록

| 명령어                | 설명                             | 인자 (Argument)      | 비고       |
|------------------------|----------------------------------|------------------------|------------|
| `fctrl on, cnd=`       | 힘 제어 시작 및 조건 적용         | `cnd=설정 번호`        | 제어 시작 시 필수 |
| `fctrl control, cnd=`  | 제어 조건 변경                | `cnd=설정 번호`        | 실행 중 제어 조건만 변경 |
| `fctrl off`            | 힘 제어 종료                      | 없음                   | - |
| `fctrl motion_on`      | 레스터 모션 시작                  | 없음                   | 설정된 레스터 모션 적용 |
| `fctrl motion_off`     | 레스터 모션 종료                  | 없음                   | 즉시 중지 |
| `motion_state`         | 레스터 모션 상태 확인             | 없음                   | - |
| `contact_state`         | 접촉면 상태 확인             | 없음                   | - |
| `cfo(crd,type)`         | 현재 FT 센서 값 수신             | `crd=cnd에서 설정한 좌표계`, `type="sensor"`                     | cnd에서 설정한 좌표계와 crd값이 동일 하지 않으면 cfo값은 update되지 않고, FT센서 값을 수신하기 위해서는 type은 무조건 "sensor"로 설정 |

<br>

[__SOURCE](3-roblang/1-example-2-axis-directional-force-control.md)
## 3.1 예제: Z축 방향 힘 제어 

다음은 로봇에서 **센서 기반 힘 제어 기능**을 사용하는 Job 프로그램 예시입니다.  
이 예제는 **Z축 방향으로 가해지는 외력이 35N 이상**이 될 때까지 대기한 후,  
작업을 수행하는 구조로 설계되어 있습니다.

<br>

---

### 📄 동작 개요

> 💡 `delay` 명령어를 통해 힘 제어 시작 전에 시간을 확보하면,  
> 센서 노이즈나 초기 진동이 억제되어 보다 안정적인 힘 제어가 가능합니다.

> ⚠️ `cfo` 명령어에서 좌표계 선택시,  
> 힘 제어 설정 선택한 좌표계와 동일 해야 힘 정보를 수신할수 있습니다.

<br>

---

### 📁 Job File 정보



 

```python
delay 1.0                           # 제어 시작 전 안정화 대기
fctrl on,cnd=2                      # 힘 제어 시작 (설정 번호 2 사용)
delay 0.5

#get_current_force
var force = cfo("tool", "sensor")   # 현재 툴 기준 외력 정보 수신

# 조건 반복: Z축 외력이 35N 이상 될 때까지 대기
if abs(force.z) < 35 then *get_current_force
delay 5                             # 외력 안정화 대기
fctrl off                           # 힘 제어 종료
                            
[__SOURCE](3-roblang/2-example-change-control-settings.md)
## 3.2 예제: 제어 설정 변경 

다음은 로봇에서 **힘 제어 조건 중 '제어(Control)' 항목만 변경**하는 Job 프로그램 예제입니다.  
이 방식은 실시간 작업 중 외력 반응 특성만 바꾸고, 필터/좌표계/모션 설정 등은 그대로 유지하고 싶을 때 유용합니다.

<br>

---

### 📄 동작 개요

- `fctrl on,cnd=2`: 힘 제어 시작 시, 전체 설정값 세트 2번을 적용
- `fctrl control,cnd=1`: **제어(Control) 항목 만** 1번 세트로 변경  
  (좌표계, 필터, 모션 등은 2번 상태 그대로 유지)

<br>

---

### 📁 Job File 정보

 

```python
delay 1.0                           # 제어 시작 전 안정화 대기
fctrl on,cnd=2                      # 힘 제어 시작 (설정 번호 2 사용)
delay 0.5

fctrl control,cnd=1                # 힘 제어 설정(제어) 변경 (설정 번호 1 사용)

delay 5                             # 외력 안정화 대기
fctrl off                           # 힘 제어 종료

[__SOURCE](3-roblang/3-example-motion-contact.md)
## 3.3 예제: 접촉면 판단 기능 

다음은 로봇에서 **접촉면 판단 기능**을 사용하는 Job 프로그램 예시입니다.  

<br>

---
### 설정 예시 

**설정**에서 **contact check** 조건을 아래와 같이 설정 합니다. 

> 예: 5초 동안 아래 기준으로 접촉을 유지하면, 접촉면 판단 OK

| 항목             | 설정 값 |
|------------------|---------|
| 힘 오차           | `3 N` |
| 힘 방향 변화량      | `20 deg` |
| 접촉 각도         | `40 deg` |
| 유지 시간             | `5 sec` |

<br> 

---

### 📁 Job File 정보


```python
delay 1.0                            # 제어 시작 전 안정화 대기
fctrl on,cnd=1                       # 힘 제어 시작 (설정 번호 1 사용)
delay 0.5                            # 접촉면 판단 조건 실행 전 까지 0.5초 대기 

wait contact_state()                 # 접촉면 판단 조건 실행, 접촉면 판단 OK 까지 대기

fctrl off                            # 힘 제어 종료


[__SOURCE](3-roblang/4-example-motion-spiral.md)
## 3.4 예제: 나선형 모션 

다음은 로봇에서 **나선형 모션**을 사용하는 Job 프로그램 예시입니다.  
이 예제는 **Z축 방향으로 가해지는 외력을 20N**로 유지하면서,  
작업을 수행하는 구조로 설계되어 있습니다.

<br>

---
### 📄 동작 개요

> ⚠️ **설정**에서 **motion type**을 나선형(Spiral)으로 설정해야 합니다. 

> ⚠️ `motion_on` 명령어 시작 전 `delay` 명령어를 필수적으로 넣으면,  진동 억제 효과로 보다 안정적인 힘 제어가 가능합니다. 

<br> 

---

### 📁 Job File 정보


```python
delay 1.0                            # 제어 시작 전 안정화 대기
fctrl on,cnd=1                       # 힘 제어 시작 (설정 번호 1 사용)
delay 0.5                            # 나선 모션 시작 전 대기

fctrl motion_on                      # 나선 모션 시작
wait motion_state() == 0             # 나선 동작 종료까지 대기
fctrl motion_off                     # 나선 모션 종료

fctrl off                            # 힘 제어 종료


[__SOURCE](4-monitoring/README.md)
# 4. 모니터링

센서 기반 힘 제어 기능이 동작 중일 때,  
사용자는 UI를 통해 다음과 같은 상태 정보를 실시간으로 확인할 수 있습니다.

[__SOURCE](4-monitoring/1-force-data-monitoring.md)
## 4.1 힘 데이터 모니터링

작업 중 로봇에 가해지는 외력을 실시간으로 확인할 수 있는 기능입니다.  
해당 정보는 Force Control 기능 사용 시 필수적으로 확인해야 합니다.

--- 

![](../_assets/_16_fctrl_ctrl_panel_force_data.png)

| 항목명       | 설명 |
|--------------|------|
| **Cartesian**| 외력 힘(N 또는 Nm)|
| **Joint**    | 힘 제어에서는 사용하지 않은 항목 |

> ⚠️ Cartesian 외력의 좌표계 기준은 설정(`cnd`)에서 선택한 좌표계를 따른다.  
> ⚠️ fctrl On 상태에서만 데이터가 갱신된다.

### TP 화면 이동 경로

**[창조정]** → **[선택]** → **[힘 데이터 모니터링]**


[__SOURCE](4-monitoring/2-force-motion-monitoring.md)
## 4.2 힘 모션 모니터링

![](../_assets/_17_fctrl_ctrl_panel_force_motion.png)

| 항목명       | 설명 |
|--------------|------|
| **Fext**     | 힘 오차(목표 힘과 외력 간의 오차) [N 또는 Nm]|
| **Cmd**      | 힘 제어를 위한 명령 위치 (mm 또는 deg) |

> ⚠️ `Fext`와 `Cmd`의 좌표계는 설정(`cnd`)에서 선택한 좌표계 기준을 따른다.

> ⚠️ 해당 데이터는 fctrl on 상황에서만 동작 

### TP 화면 이동 경로

**[창조정]** → **[선택]** → **[힘 모션 모니터링]**
[__SOURCE](5-error/README.md)
# 5. 에러 및 트러블슈팅

센서 기반 힘 제어 기능이 수행되는 동안,  
사용자는 UI를 통해 아래와 같은 **힘 제어 상태 및 예외 상황**을 실시간으로 확인할 수 있습니다.

---

### 힘 제어 예외 상황 및 대응 가이드

| 에러 코드 | 주요 원인 | 가이드 |
|:--:|---|---|
| E0260 | 힘 제어 설정 비활성 | 힘 제어 환경 설정에 On으로 설정 |
| E0353 | 힘 제어 툴 번호 오류 | 힘 제어 툴 번호 설정 |
| E1336 | 사용자 좌표계 번호 오류 | 사용자 좌표계를 설정 및 추가 |
| E0259 | FT 센서 통신 문제 | 센서/케이블 점검 |
| E0272 | 지원되지 않는 센서 | 고객센터 문의(센서 IF 필요) |
| E0273 | FT 센서 통신 문제 | 센서/케이블 점검 |
| E0274 | FT 센서 통신 문제 | 센서/케이블 점검 |


> 모든 예외 상황 발생 시 시스템은 **일시 정지(Safe-Stop)**를 우선 수행합니다.

---

### 트러블슈팅 Workflow

1. FT 센서 연결 상태 점검  
2. 툴 번호, 좌표계 번호 재검증  
3. 센서 모델 지원 여부 확인  
4. 로그 확인 및 백업 

---