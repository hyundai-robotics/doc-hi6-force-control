
[__SOURCE](README.md)
# ${cont_model} 제어기 기능설명서 - 센서 기반 힘제어 

[__SOURCE](0-about-this-manual/README.md)
# 이 설명서에 대하여


[__SOURCE](0-about-this-manual/precautions.md)
# 사전 주의사항

{% include file="ko/precautions.md" %}

[__SOURCE](0-about-this-manual/safety-notice.md)
# 안전 주의 사항

{% include file="ko/safety-notice.md" %}

[__SOURCE](1-intro/README.md)
# 1. 개요

본 매뉴얼은 센서 기반 힘 제어 시스템의 사용 방법을 안내하기 위한 문서입니다.  
본 시스템은 로봇에 장착된 힘/토크 센서를 활용하여 외력에 반응하는 제어를 수행하며,  
보다 정밀하고 안전한 접촉 작업을 가능하게 합니다.

본 매뉴얼에서는 다음과 같은 기능을 다룹니다

- 센서 환경 설정 방법 
- 툴 데이터 설정 방법
- 힘 제어 조건 설정 항목 
- 명령문 
- 모니터링 구성 
- 기본 예제

이 문서는 사용자 또는 유지보수 엔지니어가  
기능을 설정하고 동작 상태를 이해하는 데 필요한 모든 절차를 설명합니다.

해당 기능은 V60.32-01 버전 부터 사용 가능하고, 기능 라이센스가 별도로 필요 합니다. ## 2 환경 설정

센서 기반 힘 제어 기능을 사용하기 위해 아래의 주요 항목들을 설정해야 합니다. 이 항목들은 제어기 모든 동작의 **기준**이 됩니다.

---

##### 설정 메뉴 진입 경로
힘 제어 환경 설정은 아래 경로를 통해 진입할 수 있습니다: 

[F2: 시스템] - 4: 응용파라미터 - 24: 힘제어 - 1: 사용자 환경 설정 ➔ **[통신] 탭 선택**

---

##### **기능 사용 여부 설정**

센서 기반 힘 제어 기능의 활성화 여부를 설정합니다. 힘 제어 기능을 정상적으로 사용하려면 **반드시 '유효'로 설정**해야 합니다.

* **유효:** 기능 활성화
* **무효:** 기능 비활성화

##### **설정 화면 예시**

* **기능 비활성화 상태 (무효)**
  ![기능 무효 설정](../_assets/_19_fctrl_func_off.png)

* **기능 활성화 상태 (유효)**
  ![기능 유효 설정](../_assets/_20_fctrl_func_on.png)


기능 사용 여부를 **'유효'**로 설정한 후, 이어서 센서의 통신 정보와 설치된 오프셋 길이를 설정하십시오.

{% hint style="info" %}

**참고**

기능 사용 여부가 '유효'로 변경되어야 후속 센서 기본 설정 및 캘리브레이션 단계를 진행할 수 있습니다.

{% endhint %}## 2.1 F/T 센서 설치 

정밀한 힘 제어를 위해 F/T 센서를 장착할 때, 센서의 물리적인 부착 방향과 로봇 제어기 내의 센서 좌표계 설정은 반드시 완벽히 일치해야 합니다. 

만약 센서 장착 방향이나 좌표계 설정이 어긋날 경우, 제어기가 인식하는 힘/토크 방향이 실제와 달라져 힘 제어 루프가 발산하거나 로봇이 오동작할 수 있습니다.

---


##### **F/T 센서 좌표계 정의 및 일치화**

* **제어기 기준 센서 좌표계 규칙:** 자사 로봇 제어기에서 정의하는 기본 센서 좌표계의 $X, Y, Z$ 축 방향은 아래 그림과 같습니다.
* **물리적 방향 일치화:** 원형 F/T 센서 본체 측면에 각인(또는 인쇄)되어 있는 고유의 $X, Y, Z$ 축 인덱스 라인이 로봇의 센서 좌표계 방향과 정확히 일치하도록 배향하여 조립해야 합니다.

![](../_assets/_05_fctrl_ctrl_sensor_crd.png)

* **좌표계 기준 자세:** 위 매뉴얼 그림은 로봇이 표준 기본 자세일 때를 기준으로 정의된 센서 좌표계입니다.
* **조립 시 유의사항:** 센서 기구 조립 시, 케이블 인출 방향이나 센서 고정 핀 위치를 매뉴얼 상의 축 정의와 대조하여 역방향이나 위상이 틀어지지 않도록 장착하십시오.

---

{% hint style="info" %}

**툴 부하 정보 기준:** 동적 부하 식별 및 툴 부하 정보 등록 시, 무게 중심 위치는 로봇 플랜지 좌표계가 아닌 **F/T 센서 기준 좌표계 원점**을 기준으로 측정 및 입력해야 합니다.

**센서 제로 오프셋(Bias) 보정 전 필수 확인:** 센서 신호의 제로 오프셋(Bias) 보정을 수행하기 전에, 반드시 센서 진단 화면에서 로봇 툴을 특정 방향으로 밀었을 때 해당 축의 힘($F_x, F_y, F_z$) 부호(+/-)가 실제 방향과 맞게 들어오는지 정성적으로 먼저 확인하십시오.

{% endhint %}## 2.2 F/T 센서 통신 설정

실제 힘 제어(Force Control) 구동 시 제어 루프의 기준이 되는 힘/토크(F/T) 센서와의 실시간 데이터 통신을 활성화하고 연결 파라미터를 설정하는 항목입니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [1: 시스템 환경] ➔ **[통신] 탭 선택**

---

![](../_assets/_20_fctrl_func_on.png)


##### 주요 설정 항목 가이드

* **기능 사용**
  * 센서 통신 루프의 활성화 여부를 선택합니다. 힘 제어 기능을 사용하려면 반드시 **[유효]** 로 설정해야 합니다.

* **센서 타입 (Sensor Type)**
  * 하드웨어 인터페이스에 매칭되는 F/T 센서 제조사 프로필을 선택합니다. *(※ ATI, OnRobot, Robotiq, AIDIN 등 세부 제어 사양은 하위 매뉴얼 참조)*

* **통신 프로토콜 (Protocol)**
  * 센서 컨트롤러와 로봇 제어기 간의 패킷 송수신 방식을 지정합니다. (`UDP`, `TCP`,`SCI`)

* **바이어스 (Bias)**
  * 센서 초기화 시점의 툴(Tool) 자중 및 기본 잔류 노이즈를 상쇄하기 위한 오프셋 필터 기능입니다.
  * **⚠️ 주의:** 현재 버전에서는 UI 컴포넌트만 배치된 **비활성화(Disabled) 상태**입니다. 추후 지원 가능한 센서 모델에 한하여 순차적으로 기능을 지원할 예정입니다 (향후 버전 릴리즈 노트 참조).

* **네트워크 파라미터 (Network Parameters)**
  * **IP 주소:** F/T 센서 컨트롤러에 할당된 고유 IP 주소를 입력합니다.
  * **로컬 포트 (Local Port):** 로봇 제어기 측에서 센서 데이터를 수신하기 위해 개방하는 포트 번호입니다. (※ 주로 50001, 50100 사용)
  * **원격 포트 (Remote Port):** F/T 센서 측에서 데이터를 송신할 때 사용하는 포트 번호입니다. (※ 센서 제조사 사양서에 명시된 고정 포트 번호 입력)## 2.2.1 ATI 환경 설정

ATI 센서의 모델별 설정 방법 및 통신 파라미터는 다음과 같습니다.

---

##### **[기본 통신 파라미터]**
모든 ATI 센서 모델은 아래의 동일한 통신 설정을 사용합니다.

* **통신 프로토콜:** UDP
* **IP 주소:** 192.168.1.2
* **원격 포트:** 49152

---

##### **[모델별 설정 방법]** 

<br>

##### **1. ATI (일반 모델)**
모델을 별도로 개별 등록하지 않고, **ATI에서 제공하는 모델별 스케일링 값을 사용자가 직접 수동으로 입력**하여 적용하는 방식입니다.

![](../_assets/_01_04_fctrl_env_setting_ATI_Generic_UDP.png)

---

##### **2. ATI (Delta-SI-660-60)**
제어기에 사전 등록된 Delta 모델 전용 프로파일을 사용하여, 해당 모델에 최적화된 스케일링 값이 자동으로 적용됩니다.

![](../_assets/_01_01_fctrl_env_setting_ATI_Delta_UDP.png)

---

##### **3. ATI (Theta-SI2500-400)**
제어기에 사전 등록된 Theta 모델 전용 프로파일을 사용하여, 해당 모델에 최적화된 스케일링 값이 자동으로 적용됩니다.

![](../_assets/_01_02_fctrl_env_setting_ATI_Theta_UDP.png)

---

##### **4. ATI (Omega-SI7200-1400)**
제어기에 사전 등록된 Omega 모델 전용 프로파일을 사용하여, 해당 모델에 최적화된 스케일링 값이 자동으로 적용됩니다.

![](../_assets/_01_03_fctrl_env_setting_ATI_Omega_UDP.png)

{% hint style="info" %}

일반(Generic) 모델을 제외한 Delta, Theta, Omega 모델은 사전 정의된 파라미터를 제공하므로, 스케일링 값 오입력으로 인한 오작동을 방지할 수 있습니다.

{% endhint %}## 2.2.2 OnRobot 환경 설정

OnRobot 센서의 설정 방법 및 통신 파라미터는 다음과 같습니다.

---

##### **[기본 통신 파라미터]**
OnRobot 센서를 제어기와 연결하기 위해 아래의 설정을 정확히 입력하십시오.

* **통신 프로토콜:** UDP
* **IP 주소:** 192.168.1.1
* **원격 포트:** 49152

---

##### **OnRobot HEX (HEX-E / HEX-H) 설정**

OnRobot HEX 센서 모델을 사용하기 위한 환경 설정 화면입니다. 통신 파라미터 입력 후 설정이 정상적으로 반영되었는지 확인하십시오.

![](../_assets/_02_fctrl_env_setting_OnRobot_HEX_E_UDP.png)

## 2.2.3 Robotiq 환경 설정

Robotiq 센서(FT 300S 등)는 시리얼 통신(SCI) 방식을 사용하므로, 힘 제어 환경 설정과 시리얼 포트 설정을 모두 완료해야 합니다.

---

##### **[1단계: 힘 제어 센서 설정]**
우선 힘 제어 사용자 환경 설정 화면에서 통신 프로토콜을 시리얼 통신(SCI)로 지정합니다.

![](../_assets/_03_01_fctrl_env_setting_Robotiq_FT300S_SCI.png)

---

##### **[2단계: 시리얼 포트 환경 설정]**
시리얼 통신을 활성화하기 위해 아래 경로로 이동하여 제어기의 통신 포트 파라미터를 설정하십시오.

> **[F2: 시스템]** → **2: 제어파라미터** → **3: 시리얼포트** → **1: 환경설정**

![](../_assets/_03_02_fctrl_env_setting_Robotiq_FT300S_SCI_CFG.png)

{% hint style="info" %}

Robotiq 센서는 별도의 IP 주소 입력 대신, 제어기 후면 또는 내부의 물리적인 시리얼 포트(SCI) 채널 및 통신 속도(Baud rate) 매칭이 필수적입니다.

1단계 센서 지정 후, 반드시 2단계 시리얼 포트 설정을 누락 없이 진행해야 통신 에러가 발생하지 않습니다.

{% endhint %}## 2.2.4 AIDIN 환경 설정

AIDIN 센서의 설정 방법 및 통신 파라미터는 다음과 같습니다.

---

##### **[기본 통신 파라미터]**

AIDIN 센서를 제어기와 연결하기 위해 아래의 설정을 정확히 입력하십시오.

* **통신 프로토콜:** UDP
* **IP 주소:** 192.168.1.199
* **원격 포트:** 50000

---

##### **AIDIN 센서 설정**

AIDIN 6축 힘/토크 센서 모델을 사용하기 위한 환경 설정 화면입니다. 지정된 IP 주소와 원격 포트 값을 오타 없이 입력한 후 설정을 저장하십시오.

![](../_assets/_21_fctrl_env_set_AIDIN.png)

{% hint style="info" %}

AIDIN 센서는 다른 센서류와 달리 **원격 포트 번호로 `50000`을 사용**합니다. 제어기 기본값이나 타사 센서 포트(49152)를 그대로 사용할 경우 통신이 연결되지 않으므로 주의하십시오.

할당된 고유 IP 주소(`192.168.1.199`)가 제어기 네트워크 대역 내에서 충돌하지 않는지 확인 후 설정을 진행하십시오.

{% endhint %}## 2.3 F/T 센서 오프셋 설정

실제 힘 제어(Force Control) 구동 시 제어 루프의 기준이 되는 **작업 좌표계**를 설정하고, 로봇 플랜지로부터 센서까지의 위치 오프셋을 입력하는 항목입니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [1: 시스템 환경] ➔ **[오프셋] 탭 선택**

---

![](../_assets/_22_fctrl_env_set_offset_sensor.png)

##### 힘 제어 구동 프레임 설정

힘 제어 알고리즘이 동작할 때 목표 힘(Target Force)을 추종 하기 위한 센서 데이터 기준 좌표계를 지정합니다. 

* **센서(Sensor) 프레임 기준:**
  로봇 끝단에 장착된 **F/T 센서 고유의 좌표계(Sensor Frame)**를 기준으로 힘 제어를 수행합니다. 툴 끝단(TCP)을 기준으로 힘을 제어해야 하는 환경에는 적합하지 않습니다.
* **툴(Tool) 프레임 기준:**
  F/T 센서에서 측정된 힘/토크 데이터를 **최종 TCP 끝단 좌표계로 변환**하여 힘 제어를 수행합니다. 툴 끝단을 기준으로 정밀한 힘 제어가 필요할 때 선택합니다.

--- 

##### 툴 프레임 설정을 위한 필수 조건

구동 프레임을 **'툴 프레임'** 기준으로 설정하여 정상적인 좌표 변환 연산을 수행하기 위해서는 아래 **2가지 기하학적 파라미터가 반드시 선행 설정**되어야 합니다.


##### 1. 센서 오프셋 길이 설정
로봇 플랜지면을 중심으로부터 F/T 센서 좌표계 중심까지의 정밀한 물리적 거리(Z)를 입력합니다. 현재는 플랜지 회전 중심축과 센서의 중심이 일치하는 상황에서만 기능을 지원 합니다. 

* **설정 범위:** `0.0` ~ `1000.0` (mm)

![](../_assets/_23_fctrl_env_set_offset_tool.png)


##### 2. 모션 툴 데이터(Tool Data) 설정
힘 제어가 실행되는 해당 프로그램 스텝(Move 명령)에 지정된 툴 번호의 **TCP 길이 및 방향**이 정확하게 입력되어 있어야 합니다. 제어기는 이 데이터를 기반으로 플랜지에서 TCP 끝단까지의 상대 좌표를 계산합니다.

[F2: 시스템] ➔ [3: 로봇 파라미터] ➔ [1: 툴 데이터]

![](../_assets/_24_fctrl_env_set_offset_motion_tool_data.png)

{% hint style="warning" %}

구동 프레임을 **툴 프레임**로 설정한 상태에서 유효하지 않은 툴 번호를 사용하거나, 툴 데이터(TCP) 값이 실제와 다를 경우, 좌표 변환 오차로 인해 힘 제어 구동 시 시스템이 오동작하거나 발산할 수 있습니다.

파라미터 설정을 변경한 후에는 반드시 화면 하단의 **[적용/확인]** 버튼을 눌러 제어기에 최종 반영합니다.

{% endhint %}



## 3. 툴 데이터

힘 제어 기능을 사용하기 전 반드시 수행해야 하는 기본 설정입니다. F/T 센서 측정값에서 툴(Tool) 자체의 무게 성분을 제거하여, 환경과 접촉할 때 발생하는 **순수한 힘/토크만 정밀하게 계산**하기 위한 기반이 됩니다.

---

##### 3.1 툴 데이터 설정

로봇 플랜지 종단에 장착된 툴의 물리적 사양을 사용자가 직접 등록하는 단계입니다.

---

##### 3.2 동적 부하 식별 기능 (Dynamic Load Identification)

툴의 정확한 사양을 알기 어려운 경우, 로봇의 자동 모션을 통해 물리 파라미터를 직접 측정하는 기능입니다.
## 3.1 툴 데이터 설정

로봇 플랜지 종단에 장착된 툴의 물리적 사양 및 오프셋 값을 등록하여 힘 제어 알고리즘의 정확도를 향상시키는 단계입니다. 센서가 인식하는 힘과 토크는 설정된 툴 무게 및 중심 값에 따라 실시간으로 보정됩니다.

힘 제어 툴 정보 설정은 아래 경로를 통해 진입할 수 있습니다: 

[F2: 시스템] - 4: 응용파라미터 - 24: 힘제어 - 2: 툴 데이터

---

![](../_assets/_04_fctrl_ctrl_tool_data.png)

| 항목         | 설명 |
|--------------|------|
| **이름** | 툴 데이터의 식별 번호 및 이름 (자동 입력) |
| **설명** | 툴에 대한 설명 또는 용도 (선택 입력) |
| **중량 [kg]** | 장착된 툴의 실제 무게. 힘 오차 계산 시 중력 보정(Gravity Compensation)의 기준값으로 사용됨 |
| **중심 [X, Y, Z]** | F/T 센서 좌표계 기준의 툴 무게중심 위치 (단위: mm) |
| **힘 영점 [X, Y, Z]** | 툴 장착 상태에서 센서 자체의 초기 힘(Force) 오프셋을 상쇄하기 위한 영점 기준값 (단위: N) |
| **토크 영점 [X, Y, Z]** | 툴 장착 상태에서 센서 자체의 초기 토크(Torque) 오프셋을 상쇄하기 위한 영점 기준값 (단위: Nm) |
| **센서 좌표** | 센서 기준 좌표계의 방향 정의 (UI 우측 그래픽 가이드 참조) |
| **동적 부하 식별** | F/T 센서 앞 단에 설치된 툴의 무게와 중심을 추정하는 기능 |

{% hint style="info" %}
 
 - V70.02-00이상 부터 툴 정보는 최대 4개 까지 설정 가능 합니다. 
 - Hi5a에 제공된 센서기반부하추정 기능은, V70.02-00이상 버전 부터 "동적 부하 식별" 기능으로 대체 되었습니다. 

{% endhint %}## 3.2 동적 부하 식별 기능 

로봇 말단에 장착된 F/T 센서의 데이터를 실시간으로 분석하여, 부착된 툴(Payload)의 물리적 특성인 **질량(Mass), 무게 중심(CoG), 그리고 센서 바이어스(Sensor Bias)**를 일련의 캘리브레이션 모션을 통해 정밀하게 식별하는 기능입니다.

{% hint style="info" %}

해당 기능은 제어기 소프트웨어 **V70.02-00 이상** 버전부터 지원됩니다.

{% endhint %}

동적 부하 식별 기능은 아래 경로를 통해 진입할 수 있습니다:

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [2: 툴 데이터] ➔ **[F1: 동적 부하 식별]**

---

![](../_assets/_25_fctrl_dyna_id_main.png)



##### UI 설정 및 상태 표기 항목


| 실행 항목 | 설명 |
| :--- | :--- |
| **힘 제어 툴 번호** | 동적 부하 파라미터를 식별 및 저장할 대상 번호입니다. **물리적으로 F/T 센서 전단(앞단)에 부착된 순수 툴(Payload)**을 의미합니다. |
| **로봇 모션 툴 번호** | 동적 식별 모션 구동 시 기준이 되는 툴 번호입니다. **물리적으로 F/T 센서 자체의 위치 및 무게까지 포함하는 통합 툴 정보**를 의미합니다. |
| **현재 각도 / 목표 각도** | 식별 구동(확인/정상 운전) 진행 시, 각 로봇 축의 현재 실시간 각도와 매칭되는 목표 포즈 각도를 표시합니다. |
| **센서 데이터** | 구동 중 F/T 센서로부터 수집되는 실시간 힘(Force) 및 토크(Torque) 데이터 피드백을 표시합니다. |
| **Limit Min / Max** | 식별 모션이 안전하게 구동될 수 있도록 소프트웨어적으로 제한하는 로봇 축별 최소/최대 가동 한계 각도입니다. |
| **확인 운전** | 실제 부하를 식별하기 전, 로봇의 이동 경로 상의 주변 간섭 유무와 센서 케이블의 장력(당겨짐)을 사전에 점검하는 테스트 구동입니다. |
| **정상 운전** | 확인 운전이 알람 없이 정상 종료된 후 활성화되며, 실제 F/T 센서 데이터를 고속 수집하여 부하 파라미터를 계산하는 실효 식별 구동입니다. |


{% hint style="info" %}

**로봇 모션 툴 입력 순서 가이드:**

  1. **식별 전:** '로봇 모션 툴 번호'에는 대략적인 무게 정보만 우선 입력한 뒤 구동합니다.

  2. **식별 후:** 동적 부하 식별이 완료되면, 측정된 결과에 F/T 센서 자체의 자중(Weight) 및 무게중심(CoG)을 최종 합산하여 데이터를 업데이트하십시오.

{% endhint %}

---

##### 식별 모션 및 포즈 

확인 운전 및 정상 운전 실행 시, F/T 센서의 다축 중력 방향 매칭 데이터를 정밀 획득하기 위해 로봇의 손목 축(4, 5, 6축)이 사전에 정의된 6가지 기본 포즈로 순차 자동 구동됩니다.

* **P1:** `[0, 90, 0, 0, -90, 0]`
* **P2:** `[0, 90, 0, 0, 90, 0]`
* **P3:** `[0, 90, 0, 0, 0, 0]`
* **P4:** `[0, 90, 0, 0, 0, 180]`
* **P5:** `[0, 90, 0, 0, 0, 90]`
* **P6:** `[0, 90, 0, 0, 0, -90]`

![](../_assets/_26_fctrl_dyna_id_mot.png)

{% hint style="warning" %}


**수동 구동 필수 조건:** 확인/정상 운전을 실행하려면 제어기 모드 스위치가 **[수동(MANUAL)]** 상태여야 하며, 모터 ON 및 티칭 펜던트(TP)의 **이네이블 스위치(Deadman Switch)가 On(눌림)** 상태를 유지해야 합니다.

**주변 환경 점검:** 구동 시작 전, F/T 센서 케이블의 여유 길이를 확인하여 회전 시 꼬임이나 당겨짐이 없는지 점검하고, 로봇 본체 및 툴이 주변 구조물이나 안전 펜스에 충돌하지 않도록 모션 반경 내 작업 영역을 충분히 확보하십시오.

{% endhint %}

##### **작동 순서 및 절차** 

##### **1 단계: 확인 운전**
* **목적:** 추종시 로봇 움직임이 안전하게 수행될 수 있도록 주변 공간 및 간섭 여부를 먼저 점검하는 단계입니다.

* **역활:** 본격적인 식별 모션 전, 로봇이 주변 설비나 기구부와 충돌 경로가 없는지 낮은 속도로 확인합니다.

##### **2 단계: 정상 운전**
* **목적:** F/T 센서 데이터 기반으로 툴의 중량 및 중심 위치를 실시간 연산하기 위한 다축 식별 모션을 구동합니다.

* **역활:** 로봇이 사전에 정의된 각도 변위로 다양하게 자세를 변경하며 센서에 인가되는 자중 분력을 측정합니다.

##### **3 단계: 결과 확인**
* 측정이 완료되면 팝업 창을 통해 현재 적용된 `기존 값`과 센서 기반으로 새로 계산된 `추정 값`이 화면에 표시됩니다.

![](../_assets/_31_fctrl_dyna_id_result.png)

| 실행 항목 | 설명 |
| :--- | :--- |
| **무게 [Kg]** | 추정된 툴의 전체 질량입니다. |
| **중심 [mm]** | 센서 좌표계 원점 기준 툴의 X, Y, Z 방향별 무게 중심 위치입니다. |
| **힘/토크 바이어스** | 센서 고유의 제로 오프셋(Bias) 누적 값입니다. |
| **오차율 [%]** | 식별 궤적 내 데이터 신뢰도 및 오차율을 나타내며, Force와 Torque 각각 표시됩니다. |


##### **4 단계: 데이터 적용**
* 화면 우측 하단의 **[✓ OK]** 버튼을 누르면 새롭게 추정된 `추정 값` 정보가 해당 힘 제어 툴 데이터에 최종 저장 및 반영됩니다.

{% hint style="info" %}

**안전 유의 사항:** 2단계 `정상 운전` 진행 중에는 로봇이 다축 반전 거동을 수행하므로, 반드시 툴 주변의 간섭 영역을 완전히 확보한 상태에서 구동해야 합니다.

**오차율 만족 기준:** 측정된 **오차율은 반드시 5% 이내를 만족**해야 합니다.

**트러블슈팅 팁:** 만약 오차율이 5%를 초과하여 비정상적으로 높게 출력된다면 측정 중 외부 간섭이 있었거나 센서 케이블 장력이 작용한 상태일 수 있습니다. 센서 케이블의 여유 및 간섭을 재점검한 후 다시 측정하십시오.

{% endhint %}

---## 4. 힘 제어 옵션 

힘 제어 공정의 품질과 시스템 안정성을 향상시키기 위해, 작업 조건에 맞춰 센서 신호를 가공하고 로봇의 응답 및 거동 제어 방식을 세부적으로 설정하는 메뉴입니다. 

기본적인 힘/토크 제어 설정 외에도 노이즈 필터링, 프로파일, 접촉 조건 판단 및 자동 경로 생성 등의 고급 제어 옵션을 제공합니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션]

---

{% hint style="info" %}

**주의 사항:** 힘 제어 옵션의 개별 파라미터는 상호 의존적입니다. 초기 튜닝 시에는 로봇의 구동 속도 제한을 낮게 설정한 상태에서 필터 및 프로파일 성능을 확인하며 점진적으로 최적값을 설정하십시오.


{% endhint %}### 4.1 기본

힘 제어 구동 조건에 따른 이름을 설정하고, 제어 기준이 될 작업 좌표계와 툴 번호 및 센서 영점(오프셋) 기능을 설정하는 메뉴입니다. 

힘 제어 옵션 조건 설정은 아래 경로를 통해 진입할 수 있으며, 최대 15개까지 구성할 수 있습니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션] ➔ **[Settings] 탭 선택**

---

![](../_assets/_06_fctrl_ctrl_cnd_default.png)

![](../_assets/_27_fctrl_option_basic_usr.png) 

##### **기본(Settings) 설정 항목**

| 항목 | 설명 |
| :--- | :--- |
| **이름** | 힘 제어 조건의 식별 명칭입니다. 우측 리스트 목록에서 선택 시 자동으로 입력됩니다. (예: `cnd_1`) |
| **설명** | 현재 제어 조건의 용도나 작업 내용을 입력합니다. (예: `Contact`, `Sanding` 등) |
| **좌표계** | 힘 제어 루프가 적용될 기준 좌표계를 선택합니다.<br>• **베이스** <br>• **로봇** <br>• **툴** <br>• **사용자** |
| **사용자 좌표 번호** | 좌표계를 **[사용자]**로 선택했을 때 활성화되며, 적용할 사용자 좌표계의 고유 ID 번호를 지정합니다. |
| **툴 번호** | 현재 제어에 적용할 툴 데이터 번호를 선택합니다. (해당 툴에 설정된 무게 및 중심 정보가 제어 알고리즘에 실시간 연동됨) |
| **영점** | 힘 제어 시작 시 센서 데이터의 초기 오프셋을 강제로 상쇄할지 여부를 토글 스위치로 설정합니다.<br>• **On (활성화):** 구동 직전 센서 값을 0으로 리셋(소프트웨어 바이어스 처리)<br>• **Off (비활성화):** 별도의 제로 보정 없이 기존 센서 데이터 유지 |

---

{% hint style="info" %}

**사용자 좌표계 입력 주의 사항:** 사용자 좌표계를 선택한 상태에서 번호(ID)를 **미설정(None)** 으로 지정할 경우, **로봇 좌표계**와 동일하게 매칭되어 연산됩니다.

**툴 번호 연동:** 여기서 선택하는 툴 번호(ID)는 `[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [2: 툴 데이터]` 메뉴에 사전 등록된 툴 데이터 번호의 물리 파라미터를 의미합니다.

**영점 기능 Off 시 동작:** 영점 기능을 사용하지 않으면(Off), 센서 원시 출력 데이터에 미리 매칭된 툴 데이터 정보(중량, 중심, 힘/토크 영점)만을 기준으로 툴 자중을 상쇄한 정적 보정값을 사용하게 됩니다. 

{% endhint %}### 4.2 제어

힘 제어를 적용할 자유도(축)를 선택하고, 목표 힘/토크 및 가상 시스템의 응답성, 점성 저항(댐핑), 속도 및 변위 한계값을 설정하는 메뉴입니다. 실제 로봇이 환경과 접촉할 때의 유연성과 반응 속도를 결정하는 핵심 파라미터입니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션] ➔ **[Settings] 탭 선택**

---

![](../_assets/_07_fctrl_ctrl_cnd_control.png)


##### **제어(Control) 설정 항목**


| 항목 | 설명 |
| :--- | :--- |
| **축** | 제어 대상이 되는 6자유도 축을 나타냅니다. (X, Y, Z: 선속도 방향 / Rx, Ry, Rz: 회전 방향) |
| **활성** | 해당 축의 힘 제어 활성화 여부를 지정합니다. 활성화 시 박스가 **노란색**으로 점등됩니다. |
| **목표** | 목표 힘(단위: N) 또는 목표 토크(단위: Nm)를 입력합니다. |
| **응답** | 외력 또는 목표값 변화에 대한 로봇의 초기 반응성(%)을 조절합니다. 설정값이 **낮을수록 제어 반응 속도가 빨라져 목표값에 민첩하게 도달**합니다. |
| **댐핑** | 가상 점성 감쇠 계수와 매칭되는 댐핑 비율(%)입니다. 설정값이 **낮을수록 외력에 유연하게 순응**하며 반응합니다. |
| **제한** | 힘 제어 구동 시 발생할 수 있는 최대 출력 속도를 제한합니다. (선속도 축: mm/s, 회전축: deg/s) |
| **-위치 / +위치** | 힘 제어 동작 중 로봇이 강제로 밀려나거나 이동할 수 있는 양방향 가동 한계(소프트 리밋) 범위입니다. (mm 또는 deg) |


{% hint style="info" %}

**좌표계 기준:** 각 축(X, Y, Z, Rx, Ry, Rz)의 방향 정의는 앞단의 **[Settings] 탭에서 선택한 작업 좌표계**를 기준으로 매칭됩니다.

**응답 튜닝 가이드:**
  * **0% 설정 시:** 초기 접촉(Contact) 시 발생하는 충격을 완화하고, 움직임을 안정적으로 제어하여 진동을 억제하는 데 유리합니다.(다만 댐핑값이 클수록 반응이 느리게 됩니다.)
  * **1% 이상 설정 시:** 목표 제어 속도 및 추종 성능이 향상되어 더욱 민첩하게 반응합니다. 다만, 특정 범위 내에서는 설정값 변화에 따른 반응성 편차가 미세할 수 있습니다.

**속도 및 위치 제한 관리:** 응답 및 댐핑 설정의 밸런스가 맞지 않거나 속도 제한 값을 지나치게 높게 설정하면, 물체와 접촉할 때 로봇이 급가속되면서 시스템 진동을 유발할 수 있습니다. 초기 튜닝 시에는 반드시 **[제한] 속도 값을 낮게 설정**하여 안정성을 확보한 후 점진적으로 제어 성능을 높여가십시오.

{% endhint %}

---

##### **제어(Motion) 설정 예시 (수직 방향 샌딩 작업)**

실제 툴의 자세를 유지하며 가압하는 전형적인 샌딩/그라인딩 공정의 UI 설정 매칭 예시입니다.


| 축 | 활성 | 목표 | 응답 | 댐핑 | 제한 | 위치 제한 (- / +) |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **X** | Off | 0 N | 0 % | 100 % | 0 mm/s | 0 / 0 mm |
| **Y** | Off | 0 N | 0 % | 100 % | 0 mm/s | 0 / 0 mm |
| **Z** | **On** | **20 N** | **10 %** | **20 %** | **5 mm/s** | **0 / 30 mm** |
| **Rx** | **On** | **0 Nm** | **0 %** | **10 %** | **5 deg/s** | **-20 / 20 deg** |
| **Ry** | **On** | **0 Nm** | **0 %** | **10 %** | **5 deg/s** | **-20 / 20 deg** |
| **Rz** | Off | 0 Nm | 0 % | 100 % | 0 deg/s | 0 / 0 deg |

* **Z축 방향 제어:** 법선 방향(가압축)으로 **20N의 일정한 힘**을 유지하며 부드럽게 표면에 안착합니다.
* **Rx, Ry축 방향 제어:** 곡면이나 굴곡진 가공 표면 모서리에 대응하여 툴이 평평함을 유지할 수 있도록 **목표 토크 0Nm 및 저댐핑(10%)** 상태로 결합되어 부드럽게 모션 순응(Compliance)이 일어납니다.
* 좌표계는 "툴" 설정이 필수 입니다. 

---

{% hint style="warning" %}

**진동 및 소음 주의:** 댐핑 및 응답 비율을 낮추면 외부 환경 변화에 유연하게 대응하지만, 대상 물체의 정적 강성(Stiffness)이 너무 높거나 로봇의 고속 구동 조건과 결합될 경우 제어 위상 지연으로 인해 **진동 및 고주파 소음**을 유발할 수 있으므로 적절한 댐핑 마진을 확보해야 합니다.

{% endhint %}

### 4.3 필터링

F/T 센서 데이터의 신호 노이즈를 억제하여 안정적인 제어 환경을 구축하는 필터 기능과, 시스템의 추종 성능을 극대화하기 위한 민첩 모드를 설정하는 메뉴입니다. 

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션] ➔ **[Filtering] 탭 선택**

---

![](../_assets/_08_fctrl_ctrl_filtering.png)

##### **힘 필터링**

센서로부터 수집되는 힘/토크 신호의 고주파 노이즈를 감쇠시켜 로봇 동작의 흔들림을 방지하고 부드러운 접촉 거동을 제어합니다.

* **설정 방식:** 체크박스를 통해 활성화 여부를 선택합니다.

{% hint style="warning" %}

신호 품질이 우수한 F/T 센서를 사용할 때 필터 기능을 과도하게 적용하면 제어 루프 내에 위상 지연(Time Delay)이 발생할 수 있습니다. 이는 고속 힘 추종 응용 시 제어 성능 저하의 원인이 되므로, 고속 응용에서는 실제 신호 특성을 확인한 후 적용 여부를 결정하십시오.

{% endhint %}

---

##### **민첩 모드**

목표 힘에 도달하는 로봇의 초기 제어 반응 속도를 비약적으로 향상시키는 모드입니다. 툴(TCP)이 고속으로 이동하며 환경 변화에 즉각적으로 대응해야 하는 정밀 힘 제어 공정에서 필수적으로 활용됩니다.

| 설정 항목 | 설명 |
| :--- | :--- |
| **주파수 [Hz]** | 민첩 모드의 대역폭 동작 주파수를 지정합니다. 설정된 **주파수 값이 높을수록 로봇의 응답 속도가 빨라지고 민첩성**이 증가합니다. |

{% hint style="warning" %}

**민첩 모드(Agility Mode) 종료 특성**

* **주의 사양:** 민첩 모드는 `fctrl off`(종료) 시 그 자리에 무조건 **0.5초간 멈춘 뒤 종료**됩니다.

* **현장 문제:** 제품에 접촉한 채로 0.5초간 멈추면 표면에 압력이 계속 가해져 제품이 손상되거나 자국이 남을 수 있습니다.

* **보완 방법:** [5.6 예제 - 민첩 모드 종료 특성](../5-roblang/6-ex-agility-mode-end.md)

{% endhint %}

{% hint style="info" %}

**진동 및 소음 유의:** 주파수 값을 크게 설정할수록 제어계의 응답성이 급격히 정밀해지지만, 로봇의 기계적 강성 및 환경 조건에 따라 고주파 소음이나 시스템 진동을 유발할 수 있습니다. 

**안전 구동 팁:** 최초 설정 시에는 시스템 안정성을 위해 낮은 주파수 대역에서 시작하여, 로봇의 거동과 소음을 모니터링하면서 점진적으로 주파수를 높여 최적의 제어 포인트를 찾으십시오.

{% endhint %}### 4.4.1 프로파일 : 소프트 스타트

목표 힘/토크 명령을 특정 프로파일 형태로 조절하여 초기 접촉 성능을 개선하는 기능입니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션] ➔ **[Profile] 탭 선택**

---

![](../_assets/_09_fctrl_ctrl_soft_start.png)


##### **소프트 스타트**

로봇이 작업 대상물에 초기 접촉 및 진입할 때 발생할 수 있는 급격한 충격(Impact Force)을 방지하기 위한 기능입니다. 목표 제어값에 도달할 때 계단형(Step) 명령이 아닌 부드러운 곡선(Profile Curve) 형태로 목표 값을 점진적으로 인가합니다.

##### **주요 설정 항목**

| 설정 항목 | 설명 |
| :--- | :--- |
| **기능 활성화** | 기능 사용 여부를 체크 박스로 지정합니다. |
| **시작 비율 [%]** | 기능이 트리거(동작 시작)되는 시점의 힘/토크 임계값을 최종 목표치 대비 백분율(%)로 설정합니다. 센서 측정값이 이 비율에 도달하면 프로파일 제어가 시작됩니다. |
| **시간 [s]** | 시작 비율 만족 시점부터 최종 목표 힘/토크 값에 완벽히 도달할 때까지 소요되는 프로파일 커브 시간(단위: 초)을 정의합니다. |

![](../_assets/_28_fctrl_soft_start.png)

##### **동작 예시를 통한 이해**

아래 설정을 기준으로 한 실제 로봇의 튜닝 거동 예시입니다.

* **최종 목표 힘:** `10 N` (Settings 탭 Z축 설정값)
* **시작 비율:** `50 %` (최종 목표의 50%인 **5 N** 도달 시 기능 트리거)
* **프로파일 시간:** `5 s`

##### **Step 1. 초기 접촉 및 대기 (접촉 초기 단계)**
로봇이 하강하여 작업 대상물과 접촉을 시작하면 센서에 측정되는 현재 힘이 상승하기 시작합니다. 이때 실측 데이터가 트리거 기준점인 **5 N**에 도달하는 순간, 소프트 스타트 기능이 정식으로 활성화됩니다.

##### **Step 2. 프로파일 구간 진입 (프로파일 제어 단계)**
기능이 활성화된 시점부터 제어기는 명령 힘을 계단형(Step)으로 급격히 올리지 않습니다. 지정된 **5초 동안 부드러운 곡선 형태로 힘 명령을 점진적으로 증가**시키며, 최종 목표치인 10 N까지 로봇을 안정적으로 안착시킵니다.

{% hint style="info" %}

**현장 활용 팁:** * 파손 위험이 높은 부품(유리, 디스플레이 등)을 조립하거나, 정밀 가공 표면에 초기 안착할 때 접촉 충격으로 인한 제품 결함 및 스크래치를 방지하기 위해 필수적인 설정입니다.

**현장 문제 해결 가이드** * 만약 초기 진입 시 쾅 치는 충격이 여전히 발생한다면, **[시작 비율]을 더 낮추어** 기능이 더 빨리 켜지게 하거나, **[시간] 값을 늘려** 가압 곡선을 더욱 완만하게 조정하십시오.

{% endhint %}

### 4.4.2 프로파일 : 속도 제한

속도 제한 명령을 특정 프로파일 형태로 조절하여 초기 접촉 성능을 개선하는 기능입니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션] ➔ **[Profile] 탭 선택**

---

![](../_assets/_10_fctrl_ctrl_vel_clamp.png)

##### **속도 제한**

힘 제어 구동 중 로봇이 환경(물체)과 접촉할 때, 속도가 지나치게 빠르면 과도한 오버슈트와 제어계의 발산(진동)이 발생합니다. 본 기능은 현재 감지된 힘/토크 값이 사용자가 설정한 임계값을 초과하는 순간, 로봇의 최대 가동 속도(Vmax)를 더 낮은 수준으로 강제 클램핑하여 시스템의 기계적 진동과 충격을 근본적으로 억제하는 기능입니다.

##### **주요 설정 항목**

| 설정 항목 | 설명 |
| :--- | :--- |
| **기능 활성화** | 기능 사용 여부를 체크 박스로 지정합니다. |
| **선속도 - 힘 조건 [% 목표]** | 선속도 축(X, Y, Z)에 대한 트리거 조건입니다. 현재 힘이 [Settings] 탭에 설정된 최종 목표 힘 대비 설정된 백분율(%)에 도달하면 속도 제한이 즉시 수행됩니다. |
| **선속도 - 제한 비율 [% 제한 속도]** | 힘 조건 만족 시, 기존 [Settings] 탭에 설정된 축별 최대 제한 속도 대비 스케일링할 감쇠 비율(%)을 정의합니다. |
| **각속도 - 토크 조건 [% 목표]** | 회전축(Rx, Ry, Rz)에 대한 트리거 조건입니다. 현재 토크가 최종 목표 토크 대비 설정된 백분율(%)에 도달하면 속도 제한이 수행됩니다. |
| **각속도 - 제한 비율 [% 제한 속도]** | 토크 조건 만족 시, 기존 [Settings] 탭에 설정된 회전축 최대 제한 속도 대비 스케일링할 감쇠 비율(%)을 정의합니다. |

![](../_assets/_14_fctrl_ctrl_vmax_clamp.png)


##### **동작 예시를 통한 이해**

* **기본 제한 속도 (Vmax):** `2.0 mm/s` ([Settings] 탭 설정값)
* **최종 목표 힘:** `10 N` ([Settings] 탭 설정값)
* **힘 조건 [% 목표]:** `50 %` (최종 목표의 50%인 **5 N** 도달 시 기능 트리거)
* **제한 비율 [% 제한 속도]:** `30 %` (기존 속도 한계의 30%인 **0.6 mm/s**로 하향 클램핑)

---

##### **Step 1. 초기 진입 및 힘 트리거**
로봇이 하강하여 대상물과 접촉한 후, 측정되는 실측 힘 데이터가 빠르게 상승하여 트리거 기준점인 **5 N**을 넘어서는 순간 속도 제한 알고리즘이 강제로 개입합니다.

##### **Step 2. 속도 클램핑 및 진동 억제**
기능이 켜지기 전 에는 로봇이 목표 힘을 추종하기 위해 Vmax 최대치인 $\pm2.0\,\text{mm/s}$ 부근까지 진동을 보이게 됩니다. 

하지만 힘 조건이 만족되어 제한 비율이 새롭게 설정되는 순간, 로봇의 가동 속도는 사전에 지정한 **$\pm0.6\,\text{mm/s}$ (기존 속도의 30%) 한계선 내부로 강력하게 클램핑(Vmax)** 됩니다. 결과적으로 제어계의 과도한 거동이 제한되면서 하단의 힘 데이터 역시 진동을 멈추고 목표치인 10 N에 안정적으로 수렴해 나갑니다.

---

{% hint style="info" %}

**현장 튜닝 가이드 (진동 제어):** 응답 또는 댐핑 조정만으로 초기 접촉 진동(튀는 현상)이 잡히지 않을 때 본 기능을 활성화하십시오. **[제한 비율]을 적절한 수준으로 하향 조정**하면 물리적인 속도 마진이 제한되어 진동을 효과적으로 억제할 수 있습니다.

{% endhint %}

{% hint style="warning" %}

**과도한 제한 시 부작용:**
  [제한 비율]을 너무 낮게(예: 5% 이하) 설정하면 진동은 잡히지만, 로봇이 목표 힘까지 밀고 들어갈 수 있는 속도가 부족해집니다. 
  
{% endhint %}
### 4.4.3 프로파일 : 힘/토크 스케일링

F/T 센서 데이터의 미세 노이즈와 초기 접촉 충격을 억제하기 위해 불감대(Dead-zone) 및 부드러운 스케일링(Smooth Scaling)을 적용하는 기능입니다. 데이터의 급격한 변동을 방지하여 제어 안정성을 제고합니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션] ➔ **[Profile] 탭 선택**

---

![](../_assets/_11_fctrl_ctrl_smooth_scaling.png)


##### **힘/토크 스케일**

F/T 센서 데이터가 특정 크기 이하일 때는 힘 제어 루프가 반응하지 않도록 차단하고, 임계값을 넘어서면 원래의 목표 제어선까지 부드러운 곡선(Smooth Curve) 혹은 단차선 형태로 연결시켜 전반적인 접촉 안정성을 높입니다.

##### **주요 설정 항목**

| 설정 항목 | 설명 |
| :--- | :--- |
| **기능 활성화** | 기능 사용 여부를 체크 박스로 지정합니다. |
| **힘 범위 - 시작 [N]** | 데드존이 적용되는 하한값입니다. 이 값 이하의 측정값은 **0 N**으로 출력됩니다. |
| **힘 범위 - 종료 [N]** | 원래 데이터와 1:1로 동기화되는 상한값입니다. 이 값 이상의 측정값은 보정 없이 그대로 출력됩니다. |
| **토크 범위 - 시작 [Nm]** | 데드존이 적용되는 하한값입니다. 이 값 이하의 측정값은 **0 Nm**로 출력됩니다. |
| **토크 범위 - 종료 [Nm]** | 원래 데이터와 1:1로 동기화되는 상한값입니다. 이 값 이상의 측정값은 보정 없이 그대로 출력됩니다. |

---

##### **동작 예시를 통한 이해**

##### **A. 부드러운 스케일링 (시작 < 종료)**
* **설정 예시:** 시작 `5 N` / 종료 `15 N`

![](../_assets/_12_fctrl_ctrl_scaling_graph.png)

* **동작 거동:**
  1. **5 N 이하:** 데드존이 적용되어 미세 노이즈 및 진동을 차단하기 위해 **0 N**으로 출력됩니다.
  2. **5 ~ 15 N:** 데이터가 급격히 튀지 않도록 부드러운 곡선(Smooth Curve) 형태로 보간하여 출력됩니다.
  3. **15 N 이상:** 필터링 없이 원본 센서 값 그대로(1:1 선형) 제어 루프에 반영됩니다.

---

##### **B. 계단형 스케일링 (시작 ≥ 종료)**
* **설정 예시:** 시작 `5 N` / 종료 `0 N` (시작 값이 종료 값보다 크거나 같을 때)

![](../_assets/_13_fctrl_ctrl_scaling_threshold_graph.png)

* **동작 거동:**
  1. **5 N 미만:** 측정값이 임계치에 도달하기 전이므로 **0 N** 처리됩니다.
  2. **5 N 이상:** 기준 만족 즉시 원본 데이터 선으로 점프(Step)하며, 이후에는 원본 값 그대로 출력됩니다.

---

{% hint style="info" %}

**모드 자동 전환:** [시작] 값이 [종료] 값보다 크거나 같으면 곡선 보간 없이 **계단형 불감대(Threshold Dead-zone)** 모드로 동작합니다.

**현장 튜닝 팁:** 로봇 진동이나 외부 흔들림으로 인한 센서 노이즈를 차단하려면, **[시작] 값을 노이즈 최대 실측치보다 약간 높게 설정**하여 데드존을 확보하십시오.

{% endhint %}### 4.5.1 모션 : 접촉 조건

힘 제어 구동 시, 로봇 툴 끝단(TCP)이 작업 대상물의 표면에 안정적으로 접촉(안착)했는지를 실시간으로 감지하고 판단하는 기능입니다.

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션] ➔ **[Motion] 탭 선택**

---


![](../_assets/_12_fctrl_ctrl_cnd_motion_contact.png)


##### **주요 설정 항목**

아래 설정된 4가지 조건이 모두 충족되었을 때 시스템은 '접촉 완료' 상태로 판정 합니다. 

| 설정 항목 | 설명 |
| :--- | :--- |
| **힘 임계값 [N]** | 접촉을 인정하기 위한 최소 인가 힘(Force) 기준입니다. |
| **각도 변화 임계값 [각도]** | 초기 접촉 순간에 발생하는 힘 벡터 방향의 순간 변화량(변동 폭) 허용치입니다. |
| **기울기 각도 임계값 [각도]** | 작업 표면의 경사도나 툴의 기울어짐 상태에서 안정적인 접촉을 판별하기 위한 힘 벡터의 각도 허용 한계입니다. |
| **지속 시간 [초]** | 위 3가지 임계 조건(힘, 각도 변화, 기울기 각도)이 모두 만족하고 연속적으로 유지되어야 하는 최소 시간입니다. |

---

##### **설정 예시** 

* **동작 거동:** 아래 조건들이 **3초 동안 동시에 만족**하면 접촉면 안착으로 최종 판단합니다.

| 설정 항목 | UI 설정 값 | 거동 판정 기준 |
| :--- | :--- | :--- |
| **힘 임계값** | `3 N` | F/T 센서에 감지되는 외력이 3 N 이상 도달 시 |
| **각도 변화 임계값** | `10 deg` | 접촉 시 힘 방향의 순간 변화가 10° 이내로 안정화될 시 |
| **기울기 각도 임계값** | `20 deg` | 목표 각도 대비 힘 벡터의 경사 궤적이 20° 이내일 시 |
| **지속 시간** | `3 초` | 위의 상기 상태가 3초간 중단 없이 유지될 시 |

---

{% hint style="info" %}

**좌표계 설정 규칙:** 접촉 조건 알고리즘은 툴의 거동 방향과 분력을 정밀하게 매칭해야 하므로, 반드시 기준 좌표계가 **툴 좌표계(Tool Coordinate System)** 로 지정된 상태에서 정상 작동합니다.

**현장 튜닝 팁:** 로봇이 접촉 후 다음 모션으로 넘어가지 못하고 멈춰 있다면, 부딪힐 때의 충격이나 표면 굴곡 때문일 가능성이 높습니다. 이 경우 **[각도 변화 임계값]** 과 **[기울기 각도 임계값]** 을 각각 5°~10° 정도 늘려 주면 정상 동작합니다.

{% endhint %}

### 4.5.2 모션 : 자동 경로 생성

힘 제어 상태를 유지하면서 지정된 평면 상에 특정 패턴의 궤적을 자동으로 생성하며 이동하는 기능입니다.  

[F2: 시스템] ➔ [4: 응용 파라미터] ➔ [24: 힘 제어] ➔ [3: 힘 제어 옵션] ➔ **[Motion] 탭 선택**

---

![](../_assets/_15_fctrl_motion_type.png)

##### **모션 타입**

사용 목적에 따라 총 3가지의 궤적 생성 모드를 제공합니다.

* **Spiral (나선형 모션):** 중심점부터 바깥쪽으로 원을 그리며 확장되는 궤적을 생성합니다. (ex: 연마, 폴리싱 공정)
* **Bidir (양방향 모션):** 왕복 이동을 하며 면을 채우는 궤적을 생성합니다. (ex: 넓은 면적의 표면 가공)
* **Unidir (단방향 모션):** 한 방향으로만 구동 후 복귀를 반복하는 궤적을 생성합니다. (ex: 일정한 결 유지가 필요한 샌딩, 디스펜싱 공정)

---

##### **1. Spiral (나선형 모션)**

중심점에서 시작하여 동심원 형태로 반경을 넓혀가는 경로를 생성하는 기능입니다.  

| 항목명 | 설명 |
| :--- | :--- |
| **속도** | 나선 궤적을 이동하는 TCP 선속도 (단위: mm/sec) |
| **반경** | 최종 확장될 나선의 최대 반지름 (단위: mm) |
| **회전수** | 시작점부터 종료점까지 회전하는 총 횟수 (단위: rev) |

---

##### **2. Bidir (양방향 모션)**

끝단 가공 방향을 바꾸며 왕복 형태의 연속적인 선형 경로를 생성하는 기능입니다.  

![](../_assets/_16_fctrl_ctrl_motion_bidirectional.png)

| 항목명 | 설명 |
| :--- | :--- |
| **속도** | 경로를 이동하는 TCP 선속도 (단위: mm/sec) |
| **이동 방향** | 메인 가공 구동 방향 (+X, -X, +Y, -Y) |
| **이동 길이** | 메인 가공 경로의 1회 스케일 거리 (단위: mm) |
| **줄바꿈 방향** | 다음 라인으로 건너뛰는 피치(Pitch) 이동 방향 (+X, -X, +Y, -Y) |
| **줄바꿈 길이** | 다음 라인과의 간격(Pitch) 거리 (단위: mm) |
| **모서리 반경** | 라인이 꺾이는 모서리 구간의 라운딩 반경 (단위: mm) |

{% hint style="info" %}

**모서리 반경 설정 제한:** 모서리 반경의 최댓값은 [이동 길이]와 [줄바꿈 길이] 중 작은 값의 절반을 초과할 수 없습니다.

{% endhint %}

---

##### **3. Unidir (단방향 모션)**

항상 일정한 정방향 구동을 유지하기 위해, 편도 이동 후 공구를 떼고 원점으로 복귀하여 라인을 바꾸는 경로 생성 기능입니다.  

![](../_assets/_17_fctrl_ctrl_motion_unidirectional.png)

| 항목명 | 설명 |
| :--- | :--- |
| **속도** | 경로를 이동하는 TCP 선속도 (단위: mm/sec) |
| **이동 방향** | 단방향 가공 구동 방향 (+X, -X, +Y, -Y) |
| **이동 길이** | 1회 편도 가공 경로의 거리 (단위: mm) |
| **줄바꿈 방향** | 다음 라인으로 건너뛰는 피치(Pitch) 이동 방향 (+X, -X, +Y, -Y) |
| **줄바꿈 길이** | 다음 라인과의 간격(Pitch) 거리 (단위: mm) |
| **줄 개수** | 지정된 이동 방향과 줄바꿈 길이에 따라 생성할 총 라인의 수 (단위: ea) |

---

{% hint style="info" %}

**좌표계 가이드:** 모든 자동 경로 생성(Spiral, Bidir, Unidir)은 힘 제어 좌표계 설정 항목에서 지정한 **기준 좌표계의 XY 평면**을 기준으로 2차원 궤적을 계산합니다. 

{% endhint %}# 5. 명령어

이 문서는 힘 제어 기능과 관련된 주요 시스템 명령어 및 상태 변수(시스템 변수)에 대한 설명을 제공합니다. 각 명령어는 힘 제어 설정, 동작 제어, 상태 확인 및 F/T 센서 데이터 수신에 사용됩니다.

---

##### 1. 명령어 및 시스템 변수 (V70.02-00 이상)

V70.02-00 이상 버전에서는 명확한 구분을 위해 실행 명령어(`fctrl`)와 내장 상태 변수(`_fctrl`)가 분리된 표준 형식을 사용합니다.

##### **제어 및 동작 명령어**

| 명령어 | 설명 | 인자 (Argument) | 비고 |
| :--- | :--- | :--- | :--- |
| `fctrl on, cnd=` | 힘 제어 구동을 시작하고 지정된 제어 조건을 적용합니다. | `cnd=설정 번호` (예: `cnd=1`) | 힘 제어 시작 시 필수 실행 |
| `fctrl control, cnd=` | 힘 제어 구동을 유지한 상태에서 제어 파라미터 조건만 실시간으로 변경합니다. | `cnd=설정 번호` (예: `cnd=2`) | 모션 정지 없이 제어 조건 전환 시 사용|
| `fctrl off` | 힘 제어 구동을 안전하게 종료하고 일반 위치 제어로 복귀합니다. | 없음 | - |
| `fctrl motion_on` | 지정된 자동 경로 생성 모션(Spiral, Bidir, Unidir)을 시작합니다. | 없음 | `fctrl on` 상태에서만 동작 유효 |
| `fctrl motion_off` | 구동 중인 자동 경로 생성 모션을 즉시 중지합니다. | 없음 | 힘 제어(`fctrl on`) 상태는 유지됨 |

{% hint style="info" %}

**`fctrl control` 변경 가능 파라미터(사양):** 해당 명령어로 실시간 조건을 전환할 때는 제어 조건(선택 축, 활성 여부, 목표 힘, 응답성, 댐핑 가인, 제한 속도, $\pm$ 위치 제한 범위)만 변경할 수 있습니다. 기준 좌표계, 툴 번호 및 필터링/프로파일/모션 설정은 변경되지 않고 기존에 설정된(`fctrl on, cnd`) 설정을 유지 합니다. 

{% endhint %}

##### **모니터링 및 센서 데이터 수신 변수**

| 시스템 변수 | 설명 | 데이터 타입 | 비고 |
| :--- | :--- | :--- | :--- |
| `_fctrl.motion` | 현재 자동 경로 생성 모션의 구동 상태를 반환합니다. | (0: 구동 중, 1: 모션 완료) | 모션 동작 상태 여부 확인(ex: `wait _fctrl.motion==0`) |
| `_fctrl.contact` | 현재 작업 대상물 표면과의 접촉 판단 상태를 반환합니다. | (0: 미접촉, 1: 접촉 완료) | 접촉 조건 충족 여부 확인(ex: `wait _fctrl.contact`) |
| `_fctrl.force_x` | 현재 F/T 센서가 측정 중인 툴 좌표계 기준 X축 방향의 힘을 수신합니다. | (단위: N) | - |
| `_fctrl.force_y` | 현재 F/T 센서가 측정 중인 툴 좌표계 기준 Y축 방향의 힘을 수신합니다. | (단위: N) | - |
| `_fctrl.force_z` | 현재 F/T 센서가 측정 중인 툴 좌표계 기준 Z축 방향의 힘을 수신합니다. | (단위: N) | 가압 제어축 모니터링 주요 변수 |
| `_fctrl.torque_rx` | 현재 F/T 센서가 측정 중인 툴 좌표계 기준 X축 회전 토크를 수신합니다. | (단위: Nm) | - |
| `_fctrl.torque_ry` | 현재 F/T 센서가 측정 중인 툴 좌표계 기준 Y축 회전 토크를 수신합니다. | (단위: Nm) | - |
| `_fctrl.torque_rz` | 현재 F/T 센서가 측정 중인 툴 좌표계 기준 Z축 회전 토크를 수신합니다. | (단위: Nm) | - |


---

# 2. 명령어 및 함수 규격 (V70.00-00 미만)

V70.02-00 미만 하위 버전의 제어기기 및 레거시 프로그램 코드에서는 아래의 전역 변수 및 내장 함수 규격을 사용하므로 유지보수 시 주의하시기 바랍니다.

| 명령어 / 함수 | 설명 | 인자 및 세부 규칙 | 비고 |
| :--- | :--- | :--- | :--- |
| `motion_state` | 현재 자동 경로 생성 모션의 구동 상태를 반환합니다. | 없음 | V70.02-00 버전의 `_fctrl.motion`과 동일 |
| `contact_state` | 현재 작업 대상물 표면과의 접촉 판단 상태를 반환합니다. | 없음 | V70.02-00  버전의 `_fctrl.contact`과 동일 |


---

##### 3. F/T 센서 값 수신 함수

모든 소프트웨어 버전에서 호환되는 데이터 수신 함수입니다.

| 명령어 / 함수 | 설명 | 인자 및 세부 규칙 | 비고 |
| :--- | :--- | :--- | :--- |
| `cfo(crd, type)` | 지정된 좌표계 기준의 F/T 센서 데이터를 수신합니다. | `crd` : 힘 제어 조건(cnd)에 설정된 기준 좌표계 번호<br>`type` : `"sensor"` 고정 | 하단 주의 사항 참조 |


---

{% hint style="info" %}

**`cfo()` 사용 주의:** 입력한 좌표계(`crd`)가 현재 적용 중인 힘 제어 조건(`cnd`)의 기준 좌표계와 다르면 데이터가 갱신되지 않으며, 실시간 데이터 수신을 위해 `type` 인자는 반드시 소문자 `"sensor"`로 입력해야 합니다.

**사용 권장:** 인자 매칭 오류가 없는 **V70.02-00 이상의 전용 시스템 변수(`_fctrl.force_x` 등)** 사용을 강력히 권장합니다.

{% endhint %}
## 5.1 Z축 방향 힘 제어 

다음은 로봇에서 **센서 기반 힘 제어 기능**을 사용하는 Job 프로그램 예시입니다.  
이 예제는 **Z축 방향으로 가해지는 외력이 35N 이상**이 될 때까지 대기한 후,  
작업을 수행하는 구조로 설계되어 있습니다.

---

##### **동작 개요**

- delay 명령어를 통해 힘 제어 시작 전에 시간을 확보하면,  
- 센서 노이즈나 초기 진동이 억제되어 보다 안정적인 힘 제어가 가능합니다.

---

##### **JOB 프로그램 예시** 

```python
delay 1.0                           # 제어 시작 전 안정화 대기
fctrl on,cnd=2                      # 힘 제어 시작 (설정 번호 2 사용)
delay 0.5

#get_current_force
var force = _fctrl.force_z          # cnd=2에서 설정한 좌표계 기준 Z 방향 현재 힘

# 조건 반복: Z축 외력이 35N 이상 될 때까지 대기
if abs(force.z) < 35 then *get_current_force
delay 5                             # 외력 안정화 대기
fctrl off                           # 힘 제어 종료## 5.2 제어 설정 변경 

다음은 로봇에서 **힘 제어 조건 중 '제어' 항목만 변경**하는 Job 프로그램 예제입니다.  
이 방식은 실시간 작업 중 외력 반응 특성만 바꾸고, 필터/좌표계/모션 설정 등은 그대로 유지하고 싶을 때 유용합니다.

---

##### **동작 개요**

- fctrl on,cnd=2: 힘 제어 시작 시, 전체 설정값 세트 2번을 적용
- fctrl control,cnd=1: **제어 항목 만** 1번 세트로 변경  
  (좌표계, 필터, 모션 등은 2번 상태 그대로 유지)

---

##### **JOB 프로그램 예시** 

```python
delay 1.0                           # 제어 시작 전 안정화 대기
fctrl on,cnd=2                      # 힘 제어 시작 (설정 번호 2 사용)
delay 0.5

fctrl control,cnd=1                # 힘 제어 설정(제어) 변경 (설정 번호 1 사용)

delay 5                             # 외력 안정화 대기
fctrl off                           # 힘 제어 종료
## 5.3 접촉면 판단 기능 

다음은 로봇에서 **접촉면 판단 기능**을 사용하는 Job 프로그램 예시입니다.  

---

##### **동작 개요** 

**설정**에서 **contact check** 조건을 아래와 같이 설정 합니다. 

- 예시: 5초 동안 아래 기준으로 접촉을 유지하면, 접촉면 판단 OK

| 항목             | 설정 값 |
|------------------|---------|
| 힘 오차           | 3 N |
| 힘 방향 변화량      | 20 deg |
| 접촉 각도         | 40 deg |
| 유지 시간             | 5 sec |


---

##### **JOB 프로그램 예시**  

```python
delay 1.0                            # 제어 시작 전 안정화 대기
fctrl on,cnd=1                       # 힘 제어 시작 (설정 번호 1 사용)
delay 0.5                            # 접촉면 판단 조건 실행 전 까지 0.5초 대기 

wait _fctrl.contact, 20              # 접촉면 판단 조건 실행, 접촉면 판단 OK 까지 대기, 최대 20초 까지 검사 진행 

fctrl off                            # 힘 제어 종료

## 5.4 나선형 모션 

다음은 로봇에서 **나선형 모션**을 사용하는 Job 프로그램 예시입니다.  
이 예제는 **Z축 방향으로 가해지는 외력을 20N**로 유지하면서,  
작업을 수행하는 구조로 설계되어 있습니다.

---

##### **동작 개요**

- **설정**에서 **motion type**을 나선형(Spiral)으로 설정해야 합니다. 

- fctrl motion_on 명령어 시작 전 delay 명령어를 필수적으로 넣으면,  진동 억제 효과로 보다 안정적인 힘 제어가 가능합니다. 

---

##### **JOB 프로그램 예시**  

```python
delay 1.0                            # 제어 시작 전 안정화 대기
fctrl on,cnd=1                       # 힘 제어 시작 (설정 번호 1 사용)
delay 0.5                            # 나선 모션 시작 전 대기

fctrl motion_on                      # 나선 모션 시작
wait _fctrl.motion==0                # 나선 동작 종료까지 대기
fctrl motion_off                     # 나선 모션 종료

fctrl off                            # 힘 제어 종료

## 5.5 for문을 활용한 제어 조건 연속 전환

로봇 스크립트 언어의 `for` 루프 반복문을 활용하여, 여러 개의 힘 제어 조건(`cnd`)을 순차적으로 자동 전환하며 구동하는 예제입니다.

---

##### 수동 티칭 및 스텝 구동 시 주의 사항

* **변수 사전 선언 필수:** 프로그램 상에서 `fctrl on, cnd=idx_cnd`와 같이 인자값에 변수를 지정한 경우, 해당 변수가 먼저 선언 및 초기화(`var idx_cnd=1`)되어야 제어기가 정상 인식합니다.
* **에러 발생 원인:** 변수 초기화 라인을 실행하지 않은 채 수동 스텝(전/후진)으로 `fctrl on` 명령 라인만 단독 실행하면, 제어기는 `idx_cnd` 변수에 할당된 값이 유효한지(UI에 등록된 조건 번호인지) 판단하지 못하고 메모리 상의 알 수 없는 값(쓰레기 값)으로 인식하여 **시스템 에러를 발생**시킵니다.
* **조치 방법:** 수동으로 라인 동작을 테스트하거나 확인할 때는 반드시 변수 초기화 명령문(`var idx_cnd=1`)을 먼저 실행하여 변수에 정상적인 조건 번호를 할당한 후 `fctrl on` 라인을 구동하십시오.


---

##### **JOB 프로그램 예시**  

```python
S1   move P,spd=5%,accu=0,tool=0  
S2   move L,spd=5%,accu=0,tool=0   # 홈 포지션 및 접근 위치로 이동
     delay 2
     
     var idx_cnd=1                 # 힘 제어 조건 번호 변수 선언
     for idx_cnd=1 to 3            # 루프 구동: 등록된 힘 제어 조건 1번부터 3번까지 순차 적용 

       delay 3
       fctrl on,cnd=idx_cnd
       delay 20
       fctrl off
S3   move L,spd=5%,accu=0,tool=0  

     next
     
     end

## 5.6 민첩 모드 종료 특성 회피 방법

* **주의 사양:** 민첩 모드는 `fctrl off`(종료) 명령 시 해당 지점에서 무조건 **0.5초간** 멈춘 뒤 종료 되는 사양을 가집니다. 


* **현장 문제:** 가공이나 도포가 끝난 직후 표면에 접촉한 상태 그대로 제어가 종료되면, 0.5초간 가압이 유지되어 제품 표면이 손상되거나 자국이 남을 수 있습니다.

* **해결 방법:** 힘 제어 중에는 위치 보정 명령어(`shift`)를 사용할 수 없으므로, `fctrl off`를 하기 전에 **가압 방향과 반대 방향의 힘을 인가하는 회피 조건(cnd)** 으로 실시간 전환하여 로봇을 표면에서 먼저 떼어냅니다. 이후 비접촉(공중) 상태에서 0.5초 대기를 거쳐 안전하게 종료합니다.

---

##### **조건 설정 구성 (예시)**

* **메인 제어 조건 (cnd=4):** 민첩 모드를 활성화하여 표면 가공 및 자동 경로 모션을 수행하는 조건입니다.
* **회피 조건 (cnd=5):** 제품 손상을 방지하기 위해 메인 가압 방향의 반대 방향으로 이탈하도록 설정한 조건입니다.

![](../_assets/_29_agility_main_cnd.png)
*▲ 그림: 민첩 모드가 활성화된 메인 힘 제어 조건 (cnd=4) 설정 화면*

![](../_assets/_30_agility_escape_cnd.png)
*▲ 그림: 표면 이탈을 위해 반대 방향 외력을 인가하도록 설정한 회피 조건 (cnd=5) 설정 화면*


---

##### **JOB 프로그램 예시**

```python
# 홈 포지션 및 가공 접근 위치로 이동
S1   move P,spd=5%,accu=0,tool=0  
S2   move L,spd=5%,accu=0,tool=0  
     delay 2
     
# 메인 힘 제어 구동 및 자동 경로 수행
     fctrl on,cnd=4                # 메인 힘 제어(민첩 모드) 시작
     wait _fctrl.contact,20        # 표면 접촉 완료 대기 (타임아웃 20초)
     fctrl motion_on               # 자동 경로 생성 모션 시작
     wait _fctrl.motion==0         # 모션 패스 완료 시까지 대기
    
# 제품 보호를 위한 반대 방향 탈출 시퀀스
     fctrl control,cnd=5           # 접촉면 반대 방향(공중)으로 실시간 회피 조건 전환
     delay 2                       # 로봇이 표면에서 완전히 떨어질 때까지 대기
     
# 비접촉 상태에서 안전 종료
     fctrl off                     # 힘 제어 종료 (비접촉 상태이므로 0.5초 멈춤 시 제품 영향 없음)
     delay 1
  
end# 6. 모니터링

힘 제어 기능이 진행되는 동안, 로봇의 실시간 거동 상태와 F/T 센서 데이터를 직관적으로 확인하고 진단할 수 있는 모니터링 메뉴입니다. 

실시간 데이터 분석을 통해 제어 파라미터를 최적화하고, 현장 티칭 과정에서 발생할 수 있는 이상 거동을 안전하게 진단할 수 있습니다.


---


##### 모니터링 메뉴 구성

사용 목적에 따라 총 2가지의 전용 모니터링 화면을 제공합니다. 작업 조건과 디버깅 목적에 맞춰 선택하여 활용하시기 바랍니다.

##### **6.1 힘 데이터 모니터링**
* **목적:** F/T 센서가 측정하는 날것의 힘(Force)과 토크(Torque) 데이터 자체를 확인하고자 할 때 사용합니다.

* **주요 확인 항목:** 지정된 기준 좌표계 기반의 직교좌표(X, Y, Z, RX, RY, RZ) 현재 힘 데이터를 확입 합니다. 

##### **6.2 힘 모션 모니터링**
* **목적:** 실시간 가압 상태와 로봇의 추종 거동 및 속도 제약 조건, 접촉 판정 상태를 종합적으로 진단할 수 있습니다.

* **주요 확인 항목:** 실시간 목표 힘(F), 위치 명령(Cmd), 최대 제한 속도(Vmax), 접촉 조건 임계치 매칭 상태(TLT, AVG, HLD)

---

{% hint style="info" %}

**안전 모니터링 수칙:** 신규 공정 튜닝 시에는 반드시 `6.2 힘 모션 모니터링` 창의 **Vmax(최대 제한 속도)** 와 **HLD(지속 시간)** 카운트를 상시 모니터링하여, 로봇이 의도치 않은 발산 진동이나 접촉 판단 누락으로 멈춰 있는지 실시간으로 체크하는 것을 권장합니다.

{% endhint %}## 6.1 힘 데이터 모니터링

작업 중 로봇에 가해지는 외력과 토크 데이터를 실시간으로 모니터링할 수 있는 기능입니다. 힘 제어 기능 구동 시 정밀한 인가 상태를 확인하기 위해 필수적으로 참조해야 합니다.

--- 

![](../_assets/_16_fctrl_ctrl_panel_force_data.png)

##### **화면 구성 항목**

| 항목명 | 설명 |
| :--- | :--- |
| **직교좌표 (X, Y, Z, RX, RY, RZ)** | 선택된 기준 좌표계 상에서 로봇 끝단에 작용하는 3축 방향의 힘($\text{N}$) 및 3축 회전 토크($\text{Nm}$) 값입니다. |
| **축좌표 (J1, J2, J3, J4, J5, J6)** | 로봇의 각 관절축(Joint 1 ~ Joint 6)별로 인가되는 물리적인 부하 상태를 실시간으로 환산하여 모니터링하는 항목입니다. |

* **좌표계 기준:** [직교좌표] 데이터의 방향 및 기준축은 힘 제어 조건 설정(`cnd`) 메뉴에서 사용자가 지정한 기준 좌표계를 추종하여 매칭됩니다.

* **데이터 갱신 규칙:** 해당 모니터링 창은 힘 제어 구동 명령어(`fctrl on`)가 활성화되어 제어 루프가 돌고 있는 상태에서만 실시간으로 데이터가 업데이트됩니다.


--- 

{% hint style="info" %}

**티칭 펜던트(TP) 이동 경로:** [창 조정] ➔ [F1: 선택] ➔ [힘 데이터]

**현장 확인 팁:** 로봇 정지 상태에서 데이터가 `0.000` 근처가 아니라면, 자중 보정(Gravity Compensation)이나 센서 제로 세팅(Zeroing)을 확인하십시오.

**출력 기준:** 힘 제어 구동 시 **[직교좌표]** 항목에만 F/T 센서 데이터가 실시간 출력되며, **[축좌표]** 는 `0.0`으로 고정됩니다.

{% endhint %}## 6.2 힘 모션 모니터링

힘 제어 구동 중 실시간으로 인가되는 힘 데이터(F), 제어 명령 위치(Cmd), 그리고 설정된 속도 제한 값(Vmax)의 오차 및 거동 상태를 종합적으로 확인할 수 있는 기능입니다.

--- 

![](../_assets/_17_fctrl_ctrl_panel_force_motion.png)

##### **화면 구성 항목**

| 항목명 | 설명 |
| :--- | :--- |
| **F** | 실시간 목표 힘 입니다. [단위: N 또는 Nm] |
| **Cmd** | 힘 제어를 추종하기 위해 시스템에서 실시간으로 생성하는 명령 위치입니다. [단위: mm 또는 deg] |
| **Vmax** | 시스템 안정성을 위해 설정된 최대 제한 속도 프로파일 데이터입니다. [단위: mm/s] |
| **TLT** | 접촉 조건 중 **[기울기 각도 임계값]** 상태를 실시간으로 모니터링합니다. |
| **AVG** | 접촉 조건 중 **[각도 변화 임계값]** 상태를 실시간으로 모니터링합니다. |
| **HLD** | 접촉 조건 중 **[지속 시간]** 카운트 수치 및 유지 상태를 모니터링합니다. |

* **좌표계 기준:** 모니터링되는 모든 데이터의 방향 및 축은 힘 제어 조건 설정(`cnd`)에서 지정한 기준 좌표계를 기준으로 정렬됩니다.

* **데이터 갱신 규칙:** 해당 모니터링 창은 힘 제어 구동 명령어(`fctrl on`)가 활성화되어 실시간 제어 루프가 구동 중인 상태에서만 실시간으로 업데이트됩니다.

--- 

{% hint style="info" %}

**티칭 펜던트(TP) 이동 경로:** [창 조정] ➔ [F1: 선택] ➔ [힘 모션]

**현장 확인 팁:** 초기 접촉 시 로봇 거동이 불안정하다면 우측의 `TLT`, `AVG`, `HLD` 상태 모니터링 데이터를 확인하십시오. 접촉 후 `HLD`(지속 시간)가 목표치까지 채워지지 못하고 리셋된다면, `TLT`나 `AVG` 값이 순간적으로 임계치를 벗어나 접촉 판정이 누락되는 상태이므로 해당 임계 조건을 튜닝해야 합니다.

{% endhint %}# 7. 에러 및 트러블슈팅

힘 제어 공정 중 제어기 및 F/T 센서단에서 발생하는 주요 예외 상황(에러 코드)의 원인과 현장 조치 가이드입니다.

---

##### 힘 제어 예외 상황 및 대응 가이드

| 에러 코드 | 에러 명칭 및 주요 원인 | 현장 조치 가이드 |
|:--:|---|---|
| **E0260** | **힘 제어 기능 비활성화**<br>시스템 파라미터 내 힘 제어 사용 설정이 꺼져 있는 경우 | [시스템 ➔ 응용 파라미터 ➔ 힘 제어] 메뉴에서 기능 사용 설정을 **On**으로 전환하십시오. |
| **E0355** | **힘 제어 툴(Tool) 번호 오류**<br>현재 로드된 툴 번호 정보가 없거나 힘 제어 조건(`cnd`) 내 지정된 툴 설정이 불일치할 때 | 현재 티칭 프로그램에 유효한 툴 번호가 정의되어 있는지 확인하고, 힘 제어 설정 내 툴 파라미터를 재지정하십시오. |
| **E1336** | **사용자 좌표계 번호 오류**<br>힘 제어 조건(`cnd`)의 기준 좌표계가 존재하지 않는 사용자 좌표계로 지정되었을 때 | 선택한 사용자 좌표계 번호가 정상적으로 설정 및 기록되어 있는지 [좌표계 설정] 메뉴에서 검증하십시오. |
| E0259 | FT 센서 통신 문제 | 센서/케이블 점검 |
| E0272 | 지원되지 않는 센서 | 고객센터 문의(센서 IF 필요) |
| E0273 | FT 센서 통신 문제 | 센서/케이블 점검 |
| E0274 | FT 센서 통신 문제 | 센서/케이블 점검 |


* **시스템 보호 거동:** 상기 모든 예외 상황 발생 시 로봇은 즉각 가압 구동을 중단하고 **안전 일시정지(Safe-Stop)** 상태로 전환됩니다.

---

##### 트러블슈팅 진단 흐름 (Workflow)

현장에서 알 수 없는 힘 제어 에러 또는 이상 거동 발생 시 아래 단계 순으로 진단을 진행하십시오.

1. **F/T 센서 물리 연결 점검:** 센서 본체 커넥터 및 제어기 내부 수신 라인의 LED 상태 확인
2. **소프트웨어 파라미터 검증:** 현재 구동 프로그램 내부의 툴(Tool) 데이터 및 기준 좌표계 할당 번호 재검증
3. **통신 품질 및 로그 확인:** 센서 원시 데이터가 모니터링 창에 출력되는지 확인 후, 제어기 시스템 로그(`Log`) 백업 수행# 8. 릴리즈 노트 (Release Notes)

힘 제어 기능의 버전별 핵심 변경 사항 및 신규 기능 히스토리입니다.

---

##### Version: V70.02-00

* **AIDIN F/T 센서 인터페이스(IF) 추가**
  * 에이딘 로보틱스사 센서 라인업의 실시간 통신 프로토콜 정식 지원
* **F/T 센서 오프셋 및 동적 부하 식별 도입**
  * 툴 무게·중심 자동 식별 알고리즘 추가 
* **소프트 스타트 (Soft Start) 기능 적용**
  * 초기 가압 램핑 제어로 초기 접촉 순간의 오버슈트 및 기구 충격 방지
* **실시간 최대 속도 제한 (Vmax) 프로파일 반영**
  * 단차 및 표면 이탈(비접촉) 시 로봇의 급가속 및 제어 발산 차단
* **구조체 형태의 전용 시스템 변수 (_fctrl) 신설**
  * `_fctrl.force_x` 등 직관적 모니터링 변수 제공 (구버전 `cfo` 함수 호환 유지)

---

